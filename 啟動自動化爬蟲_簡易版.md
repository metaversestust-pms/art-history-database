# 🎯 自動化爬蟲系統 - 3步驟啟動

## 第一步：啟動必要服務 (Neo4j + ChromaDB)

```bash
cd art-history-database

# 啟動 Neo4j 和 ChromaDB
docker-compose -f docker-compose.crawler.yml up -d neo4j chromadb

# 等待服務啟動（約10秒）
sleep 10

# 驗證服務
docker ps
```

應該看到：
- ✅ art-neo4j (運行中)
- ✅ art-chromadb (運行中)

---

## 第二步：修改配置文件

編輯 `crawler_config.yaml`，只需改一個地方：

```yaml
rag_system:
  neo4j:
    password: "your_password"  # ⚠️ 改成你設定的密碼
```

**重要：** 密碼必須與 `docker-compose.crawler.yml` 中的 `NEO4J_AUTH` 一致！

---

## 第三步：啟動爬蟲

### 方式 A：單次執行（推薦新手）

```bash
cd art-history-database

# 安裝依賴（只需執行一次）
pip3 install requests schedule pyyaml neo4j chromadb

# 啟動爬蟲
python3 src/crawler/AutomatedCrawlerSystem.py
```

當提示選擇模式時，輸入 `1` 然後按 Enter。

### 方式 B：自動排程（24小時執行一次）

```bash
cd art-history-database
python3 src/crawler/AutomatedCrawlerSystem.py
```

當提示選擇模式時，輸入 `2` 然後按 Enter。

---

## 🎉 完成！

現在系統正在：
1. 🌐 從網路收集文藝復興和巴洛克時期的藝術史資料
2. 🔄 自動處理和清洗資料
3. 📤 直接傳送到 Neo4j 和 ChromaDB
4. 💾 保存到本地檔案

---

## 查看進度

### 即時日誌
```bash
tail -f logs/automated_crawler.log
```

### 收集的資料
```bash
ls -lh automated_crawler_data/
```

---

## 預期結果

**第一次執行（約15-30分鐘）：**
- 📚 收集 400-1000 件藝術品
- 🎨 涵蓋文藝復興和巴洛克時期
- 👨‍🎨 包含達文西、米開朗基羅、拉斐爾、卡拉瓦喬、林布蘭等大師作品

**輸出檔案：**
```
automated_crawler_data/
├── renaissance_20250113_140530.json  (文藝復興資料)
├── baroque_20250113_141020.json      (巴洛克資料)
└── crawler_stats.json                (統計資料)
```

---

## 驗證資料

### 方法1：查看 Neo4j

1. 打開瀏覽器：http://localhost:7474
2. 登入 (用戶名: neo4j, 密碼: your_password)
3. 執行查詢：
   ```cypher
   MATCH (a:Artwork)
   RETURN count(a) as total
   ```

### 方法2：查看 ChromaDB

```python
import chromadb
client = chromadb.HttpClient(host='localhost', port=8000)
collection = client.get_collection("art_history")
print(f"總計: {collection.count()} 件作品")
```

### 方法3：查看統計檔案

```bash
cat automated_crawler_data/crawler_stats.json
```

---

## 在 OpenWebUI 測試

如果您有運行 OpenWebUI：

1. 打開：http://localhost:8080
2. 嘗試問：
   - "文藝復興時期有哪些重要的藝術家？"
   - "達文西的代表作品有哪些？"
   - "巴洛克藝術的特色是什麼？"
   - "卡拉瓦喬的繪畫風格如何？"

---

## 常見問題

### Q1: 提示找不到模組？
```bash
pip3 install requests schedule pyyaml neo4j chromadb
```

### Q2: 無法連接 Neo4j？
```bash
# 檢查服務
docker ps | grep neo4j

# 重啟服務
docker-compose -f docker-compose.crawler.yml restart neo4j
```

### Q3: 想停止爬蟲？
按 `Ctrl+C`

### Q4: 想看收集了多少資料？
```bash
cd art-history-database
python3 -c "
import json
with open('automated_crawler_data/crawler_stats.json') as f:
    stats = json.load(f)
    print(f'總計爬取: {stats[\"total_crawled\"]} 件')
    print(f'傳送到RAG: {stats[\"total_sent_to_rag\"]} 件')
"
```

---

## 調整收集範圍

如果想收集更多或更少的資料，編輯 `crawler_config.yaml`：

```yaml
crawler:
  strategy:
    max_results_per_artist: 30   # 每位藝術家收集的作品數
    max_results_per_keyword: 20  # 每個關鍵詞收集的作品數
```

**建議值：**
- 測試：10 / 5
- 標準：30 / 20
- 大量：100 / 50

---

## 🚀 立即開始

```bash
# 1. 啟動服務
docker-compose -f docker-compose.crawler.yml up -d neo4j chromadb

# 2. 啟動爬蟲
cd art-history-database
python3 src/crawler/AutomatedCrawlerSystem.py
# 選擇 1 (單次執行)

# 3. 坐下來喝杯咖啡，等待資料收集完成！☕
```

就這麼簡單！🎨
