# 🚀 快速啟動自動化爬蟲系統

## 一鍵啟動（推薦）

```bash
cd art-history-database
./start_automated_crawler.sh
```

然後選擇：
- **選項 1** - 立即執行一次（測試用）
- **選項 2** - 啟動自動排程（每24小時執行）

---

## 超快速啟動（一行命令）

### 單次執行
```bash
cd art-history-database && python3 src/crawler/AutomatedCrawlerSystem.py
```

### 啟動排程
```bash
cd art-history-database && nohup python3 src/crawler/AutomatedCrawlerSystem.py > crawler.log 2>&1 &
```

---

## 重要提醒 ⚠️

### 啟動前請確認：

1. **Neo4j 已運行**
   ```bash
   docker ps | grep neo4j
   ```

2. **ChromaDB 已運行**
   ```bash
   curl http://localhost:8000/api/v1/heartbeat
   ```

3. **配置文件正確**
   - 檢查 `crawler_config.yaml`
   - 確認 Neo4j 密碼已設定

---

## 配置檢查清單 ✅

打開 `crawler_config.yaml` 確認：

```yaml
# 1. 確認重點時期
crawler:
  focus_periods:
    - renaissance   ✅
    - baroque      ✅

# 2. 確認RAG連接
rag_system:
  neo4j:
    password: "請改成你的密碼"  ⚠️
  chromadb:
    host: "localhost"
    port: 8000
```

---

## 啟動流程

```
第一次啟動建議流程：

1. 測試配置
   ./start_automated_crawler.sh
   選擇 3 (測試配置)

2. 單次執行測試
   ./start_automated_crawler.sh
   選擇 1 (立即執行一次)

3. 確認資料正確後
   ./start_automated_crawler.sh
   選擇 2 (啟動自動排程)
```

---

## 查看執行狀態

### 即時查看日誌
```bash
tail -f logs/automated_crawler.log
```

### 查看統計
```bash
./start_automated_crawler.sh
選擇 4
```

---

## 停止爬蟲

### 如果是前台運行
```bash
按 Ctrl+C
```

### 如果是背景運行
```bash
ps aux | grep AutomatedCrawlerSystem
kill [PID]
```

---

## 預期結果

### 第一次執行
- ✅ 收集約 200-500 件文藝復興作品
- ✅ 收集約 200-500 件巴洛克作品
- ✅ 自動傳送到 Neo4j 和 ChromaDB
- ⏱️ 耗時約 10-30 分鐘

### 每次排程執行
- ✅ 持續收集新資料
- ✅ 自動去重
- ✅ 擴充資料庫內容

---

## 驗證資料已收集

### 查看本地檔案
```bash
ls -lh automated_crawler_data/
```

### 查詢 Neo4j
```bash
# 進入 Neo4j Browser
http://localhost:7474

# 執行查詢
MATCH (a:Artwork)
WHERE a.period IN ['Renaissance', 'Baroque']
RETURN count(a)
```

### 查詢 ChromaDB
```python
import chromadb
client = chromadb.HttpClient(host='localhost', port=8000)
collection = client.get_collection("art_history")
print(collection.count())
```

---

## 問題排查

### 問題：找不到 Python 模組
```bash
pip3 install requests schedule pyyaml neo4j chromadb
```

### 問題：無法連接 Neo4j
```bash
# 檢查 Neo4j 是否運行
docker ps | grep neo4j

# 如果沒運行，啟動它
cd art-history-database
docker-compose up -d neo4j
```

### 問題：無法連接 ChromaDB
```bash
# 檢查 ChromaDB 是否運行
docker ps | grep chromadb

# 如果沒運行，啟動它
cd art-history-database
docker-compose up -d chromadb
```

---

## 下一步

爬蟲運行後，您可以：

1. **在 OpenWebUI 測試查詢**
   ```
   http://localhost:8080

   試試問：
   - "文藝復興時期有哪些重要的藝術家？"
   - "達文西有哪些著名作品？"
   - "巴洛克藝術的特點是什麼？"
   ```

2. **查看 Neo4j 圖譜**
   ```
   http://localhost:7474

   執行：
   MATCH (a:Artwork)-[r]-(n)
   RETURN a, r, n
   LIMIT 50
   ```

3. **調整爬取策略**
   - 編輯 `crawler_config.yaml`
   - 增加其他時期
   - 調整收集數量

---

## 🎉 就這麼簡單！

現在開始：
```bash
cd art-history-database
./start_automated_crawler.sh
```

讓系統自動為您收集藝術史資料吧！🎨
