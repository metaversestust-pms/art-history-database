# 🚀 藝術史資料擴增 - 快速開始

## 📝 摘要

您現在擁有一個完整的藝術史資料擴增與RAG整合系統！

### 建立的腳本

| 腳本名稱 | 功能 | 語言 |
|---------|------|------|
| `expand-art-history-data.js` | 🤖 整合執行所有爬蟲收集資料 | Node.js |
| `integrate-to-vector-db.py` | 🔄 向量化並整合到ChromaDB和Neo4j | Python |
| `test-rag-with-new-data.js` | 🧪 測試RAG系統整合效果 | Node.js |
| `run-data-expansion-pipeline.sh` | 🎯 一鍵執行完整流水線 | Bash |

### 支援的爬蟲

✅ **renaissance-baroque-crawler.js** - 文藝復興與巴洛克時期
✅ **specialized-art-crawler.js** - 專門藝術主題（亞洲、當代等）
✅ **harvard-art-museums-crawler.js** - 哈佛藝術博物館
✅ **europeana-crawler.js** - Europeana 文化遺產平台

### 支援的RAG策略

🔍 **Vector Only RAG** - 純向量語義檢索
🕸️ **Graph Only RAG** - 知識圖譜檢索
⚖️ **Hybrid Balanced RAG** - 向量+圖譜混合
🎯 **Advanced RAG** - 多級檢索重排
🤖 **Agentic RAG** - 智能代理推理
🔄 **Self RAG** - 自我反思迭代
📝 **Naive RAG** - 基礎檢索生成

### 支援的LLM模型

🦙 Llama 3.1 8B | 🔮 Qwen 3 8B | 💎 Gemma 3 4B
🧠 DeepSeek-R1 8B | 🤖 GPT-OSS 20B | 🕸️ Llama 3 Graph RAG

**總計：6 個 LLM × 7 個 RAG 策略 = 42 種組合**

---

## ⚡ 一鍵執行（推薦）

```bash
cd art-history-database
bash run-data-expansion-pipeline.sh
```

這將自動完成：
1. ✅ 環境檢查
2. 🤖 執行爬蟲收集資料
3. 🔄 向量化並整合到資料庫
4. 🧪 測試RAG系統

**預計耗時**: 10-30分鐘（取決於網絡速度和資料量）

---

## 🔧 分步執行

如果需要更精細的控制：

### 步驟 1: 收集資料
```bash
node expand-art-history-data.js
```

**輸出**: `data/raw/*.json` 和 `data-expansion-summary.json`

### 步驟 2: 向量化與整合
```bash
python3 integrate-to-vector-db.py --hours 24
```

**結果**: 資料已導入 ChromaDB 和 Neo4j

### 步驟 3: 測試整合
```bash
node test-rag-with-new-data.js
```

**輸出**: `rag-integration-test-results.json`

---

## 📊 檢查結果

### 查看收集的資料
```bash
# 列出資料文件
ls -lh data/raw/*.json

# 查看摘要
cat data-expansion-summary.json | jq '.'
```

### 檢查資料庫狀態
```bash
# ChromaDB 文檔數量
curl http://localhost:8001/api/v1/collections/art_history

# Neo4j 統計
docker exec neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n) RETURN labels(n)[0] as type, count(*) as count ORDER BY count DESC"
```

### 測試查詢
```bash
curl -X POST http://localhost:8007/api/v1/query \
  -H 'Content-Type: application/json' \
  -d '{
    "query": "告訴我關於文藝復興時期的藝術特點",
    "model_combination_id": "llama3.1:8b@vector_only",
    "max_results": 5
  }' | jq '.'
```

---

## 🎯 快速測試不同RAG策略

```bash
# 向量RAG
curl -X POST http://localhost:8007/api/v1/query \
  -d '{"query": "Leonardo da Vinci", "model_combination_id": "llama3.1:8b@vector_only"}' \
  -H 'Content-Type: application/json'

# 圖譜RAG
curl -X POST http://localhost:8007/api/v1/query \
  -d '{"query": "Leonardo da Vinci", "model_combination_id": "llama3.1:8b@graph_only"}' \
  -H 'Content-Type: application/json'

# 混合RAG
curl -X POST http://localhost:8007/api/v1/query \
  -d '{"query": "Leonardo da Vinci", "model_combination_id": "llama3.1:8b@hybrid_balanced"}' \
  -H 'Content-Type: application/json'
```

---

## ⚠️ 前置需求檢查

確保以下服務正在運行：

```bash
# Neo4j (port 7474, 7687)
docker ps | grep neo4j

# ChromaDB (port 8001)
docker ps | grep chromadb

# Ollama (port 11434)
docker ps | grep ollama

# RAG Manager (port 8007)
curl http://localhost:8007/health
```

如果服務未運行：
```bash
cd art-history-database
docker-compose up -d neo4j chromadb ollama
```

---

## 🔥 常用命令

### 查看所有可用的模型組合
```bash
curl http://localhost:8007/api/v1/models | jq '.models[] | {id, name}'
```

### 查看所有RAG策略
```bash
curl http://localhost:8007/api/v1/strategies | jq '.strategies[]'
```

### 查看快取統計
```bash
curl http://localhost:8007/api/v1/cache/stats | jq '.'
```

### 清空快取
```bash
curl -X POST http://localhost:8007/api/v1/cache/clear
```

---

## 📚 完整文檔

詳細的使用說明請參考：
- **資料擴增與RAG整合指南.md** - 完整詳細的使用指南
- **RAG_METHODS_OVERVIEW.md** - RAG策略詳解
- **API_DOCUMENTATION.md** - API文件

---

## 💡 小技巧

### 1. 只收集特定類型的資料

編輯 `expand-art-history-data.js`，註解掉不需要的爬蟲：

```javascript
const crawlers = [
    // { path: './renaissance-baroque-crawler.js', name: '...' },
    { path: './specialized-art-crawler.js', name: '專門藝術主題爬蟲' },
    // ...
];
```

### 2. 自定義測試查詢

編輯 `test-rag-with-new-data.js`：

```javascript
this.testQueries = [
    {
        query: '你的自定義查詢',
        expectedKeywords: ['關鍵詞1', '關鍵詞2'],
        strategies: ['vector_only', 'graph_only']
    }
];
```

### 3. 調整向量化批次大小

根據系統記憶體調整 `integrate-to-vector-db.py`：

```python
batch_size = 50  # 預設 100，可降低到 50 或增加到 200
```

### 4. 定期自動更新

添加到 crontab：
```bash
# 每週日凌晨2點執行
0 2 * * 0 cd /path/to/art-history-database && bash run-data-expansion-pipeline.sh >> logs/expansion.log 2>&1
```

---

## 🐛 常見問題

### Q: 爬蟲收集失敗？
**A**: 檢查 API 金鑰是否配置在 `.env` 文件中

### Q: 向量化失敗？
**A**: 確保 Ollama 正在運行且已安裝 `nomic-embed-text` 模型

### Q: Neo4j 連接失敗？
**A**: 檢查密碼是否正確，預設為 `arthistory123`

### Q: 測試全部失敗？
**A**: 先運行健康檢查: `curl http://localhost:8007/health`

---

## 🎉 成功標誌

當您看到以下輸出時，說明系統運行正常：

```
✅ 所有爬蟲任務完成
📊 總藝術品數量: 850+
✅ 成功向量化 850+ 個藝術品
✅ 成功添加 850+ 個藝術品到Neo4j
🧪 測試摘要
   通過: 11/12
   成功率: 91.7%
✨ 藝術史資料擴增流水線完成！
```

---

## 📞 需要幫助？

1. 查看日誌文件: `logs/*.log`
2. 檢查摘要報告: `*-summary.json`
3. 閱讀完整文檔: `資料擴增與RAG整合指南.md`

---

**祝您使用愉快！🎨**
