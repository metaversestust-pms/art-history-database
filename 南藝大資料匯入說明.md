# 南藝大及漢寶德校長資料匯入說明

## 📋 診斷結果總結

### ✅ 系統狀態檢查

經過完整診斷，發現以下狀態：

1. **✅ CLI 工具正常**: `import_local_data.py` 可以正常運行
2. **✅ 資料夾存在**: `./南藝大及漢寶德校長資料整理` 包含 6 個檔案
3. **✅ 依賴模組完整**: `src/importer` 模組正常運作
4. **✅ 容器運行中**: Neo4j, ChromaDB, Ollama 都在運行

### ⚠️ 發現的問題

**問題：ChromaDB 端口配置不匹配**
- **預設配置**: CLI 工具預設使用端口 `8001`
- **實際端口**: ChromaDB 容器運行在端口 `8000`
- **影響**: 如果不指定正確端口，ChromaDB 匯入會失敗

---

## 📁 資料夾內容

```
南藝大及漢寶德校長資料整理/
├── 20個測試LLM關於漢寶德的測試提問及簡短答案.txt  (2.4 KB)
├── 專用字.txt                                        (855 B)
├── 漢寶德校長生平.pdf                                (605 KB)
├── 漢寶德紀念館導覽手冊.pdf                          (1.1 MB)
├── 認識南藝.pdf                                      (550 KB)
└── 通用字.txt                                        (266 B)
```

**支援的檔案類型**:
- ✅ PDF: 3 個檔案 (自動 OCR 文字提取)
- ✅ TXT: 3 個檔案 (文字解析)

---

## 🚀 匯入方法

### 方法 1: 使用自動化腳本（推薦）

```bash
cd /mnt/c/Users/ssking1999/Desktop/藝術史資料庫/art-history-database

# 執行自動匯入腳本
bash import_tainan_data.sh
```

**腳本功能**：
- ✅ 自動檢查系統狀態
- ✅ 自動偵測正確的 ChromaDB 端口
- ✅ 執行完整匯入
- ✅ 顯示詳細進度和結果

---

### 方法 2: 手動執行 CLI 命令

#### 2.1 匯入到 Neo4j + ChromaDB（完整匯入）

```bash
cd /mnt/c/Users/ssking1999/Desktop/藝術史資料庫/art-history-database

python3 import_local_data.py \
    -d "./南藝大及漢寶德校長資料整理" \
    --chromadb-port 8000 \
    -v
```

#### 2.2 只匯入到 Neo4j（知識圖譜）

```bash
python3 import_local_data.py \
    -d "./南藝大及漢寶德校長資料整理" \
    --neo4j-only \
    -v
```

#### 2.3 只匯入到 ChromaDB（向量搜尋）

```bash
python3 import_local_data.py \
    -d "./南藝大及漢寶德校長資料整理" \
    --chromadb-only \
    --chromadb-port 8000 \
    -v
```

#### 2.4 匯入單一檔案

```bash
# 匯入 PDF 檔案
python3 import_local_data.py \
    -f "./南藝大及漢寶德校長資料整理/漢寶德校長生平.pdf" \
    --chromadb-port 8000 \
    -v

# 匯入文字檔
python3 import_local_data.py \
    -f "./南藝大及漢寶德校長資料整理/20個測試LLM關於漢寶德的測試提問及簡短答案.txt" \
    --chromadb-port 8000 \
    -v
```

---

## 🔧 CLI 參數說明

### 必要參數（擇一）

| 參數 | 說明 | 範例 |
|-----|------|------|
| `-f FILE` | 匯入單一檔案 | `-f data.pdf` |
| `-d DIRECTORY` | 匯入整個目錄 | `-d ./my_data/` |
| `--create-template` | 建立範例模板 | - |

### 選用參數

| 參數 | 說明 | 預設值 |
|-----|------|--------|
| `--recursive` | 遞迴處理子目錄 | False |
| `--neo4j-only` | 只儲存到 Neo4j | False |
| `--chromadb-only` | 只儲存到 ChromaDB | False |
| `--no-save` | 不儲存，僅測試 | False |
| `-v, --verbose` | 顯示詳細日誌 | False |

### 資料庫連接參數

| 參數 | 說明 | 預設值 |
|-----|------|--------|
| `--neo4j-uri` | Neo4j URI | `bolt://localhost:7687` |
| `--neo4j-user` | Neo4j 使用者 | `neo4j` |
| `--neo4j-password` | Neo4j 密碼 | `arthistory123` |
| `--chromadb-host` | ChromaDB 主機 | `localhost` |
| `--chromadb-port` | ChromaDB 端口 | `8001` ⚠️ **需改為 8000** |

---

## 🔍 驗證匯入結果

### 1. 檢查 Neo4j

```bash
# 方法 1: 使用 Web 介面
# 訪問: http://localhost:7474
# 登入: neo4j / arthistory123

# 方法 2: 使用命令列
docker exec -it art-history-neo4j cypher-shell -u neo4j -p arthistory123

# 查詢漢寶德相關節點
MATCH (n) WHERE n.name CONTAINS '漢寶德' OR n.title CONTAINS '漢寶德'
RETURN n LIMIT 10;

# 查詢南藝大相關節點
MATCH (n) WHERE n.name CONTAINS '南藝' OR n.description CONTAINS '南藝'
RETURN n LIMIT 10;

# 查看所有標籤
CALL db.labels();

# 統計節點數量
MATCH (n) RETURN labels(n) AS label, count(n) AS count;
```

### 2. 檢查 ChromaDB

```python
# 使用 Python 腳本驗證
import chromadb

# 連接到 ChromaDB
client = chromadb.HttpClient(host='localhost', port=8000)

# 查看所有 collection
collections = client.list_collections()
print(f"Collections: {[c.name for c in collections]}")

# 查詢特定 collection
collection = client.get_collection('art_history')
print(f"Total documents: {collection.count()}")

# 搜尋漢寶德相關資料
results = collection.query(
    query_texts=["漢寶德"],
    n_results=5
)
print(results)
```

### 3. 在 OpenWebUI 中測試

訪問: http://localhost:8080

**測試問題**：
1. 漢寶德出生於哪一年？（答案：1934年）
2. 漢寶德曾經擔任哪些重要職務？
3. 漢寶德在建築設計領域的成就？
4. 漢寶德紀念館位於哪裡？（答案：國立臺南藝術大學校園內）
5. 南藝大的創校校長是誰？（答案：漢寶德）
6. 漢寶德的建築設計理念是什麼？

---

## 🐛 常見問題排查

### 問題 1: ModuleNotFoundError

**錯誤訊息**:
```
ModuleNotFoundError: No module named 'src.importer'
```

**解決方法**:
```bash
# 確認在正確目錄
cd /mnt/c/Users/ssking1999/Desktop/藝術史資料庫/art-history-database

# 確認 src/importer 存在
ls -la src/importer/
```

---

### 問題 2: ChromaDB 連接失敗

**錯誤訊息**:
```
Connection refused to localhost:8001
```

**解決方法**:
```bash
# 檢查 ChromaDB 實際端口
docker ps | grep chromadb

# 使用正確端口
python3 import_local_data.py -d "./南藝大及漢寶德校長資料整理" --chromadb-port 8000
```

---

### 問題 3: Neo4j 連接失敗

**錯誤訊息**:
```
Failed to connect to Neo4j
```

**解決方法**:
```bash
# 檢查 Neo4j 狀態
docker ps | grep neo4j

# 重啟 Neo4j
docker restart art-history-neo4j

# 檢查日誌
docker logs art-history-neo4j --tail 50
```

---

### 問題 4: PDF 解析失敗

**錯誤訊息**:
```
Error processing PDF file
```

**解決方法**:
```bash
# 安裝 PDF 依賴
pip install pypdf2 pdfminer.six

# 如果還是失敗，可以先轉換成文字檔
# 或使用 --no-save 測試解析
python3 import_local_data.py -f file.pdf --no-save -v
```

---

### 問題 5: Ollama 嵌入向量生成失敗

**錯誤訊息**:
```
Failed to generate embeddings
```

**解決方法**:
```bash
# 檢查 Ollama 狀態
docker ps | grep ollama

# 檢查嵌入模型是否存在
docker exec art-history-ollama ollama list | grep nomic

# 如果沒有，下載模型
docker exec art-history-ollama ollama pull nomic-embed-text

# 只匯入到 Neo4j（跳過 ChromaDB）
python3 import_local_data.py -d "./南藝大及漢寶德校長資料整理" --neo4j-only
```

---

## 📊 預期輸出範例

### 成功匯入的輸出

```
============================================================
🎨 藝術史本地資料匯入工具
============================================================
儲存到Neo4j:     ✅ 是
儲存到ChromaDB:  ✅ 是
============================================================

============================================================
📁 掃描目錄: 南藝大及漢寶德校長資料整理
============================================================
🔍 找到 6 個支援的檔案

============================================================
📂 開始處理: 漢寶德校長生平.pdf
============================================================
📄 處理PDF檔案: 漢寶德校長生平.pdf
✅ 提取 15 頁文字
✅ 建立 5 個藝術品/文檔節點
✅ 儲存到 Neo4j
✅ 生成嵌入向量並儲存到 ChromaDB

============================================================
📂 開始處理: 20個測試LLM關於漢寶德的測試提問及簡短答案.txt
============================================================
📄 處理文字檔案
✅ 解析 20 個問答對
✅ 建立 20 個知識節點
✅ 儲存到 Neo4j 和 ChromaDB

... (其他檔案處理中) ...

✅ 總共處理 45 個節點/文檔

📊 統計資訊:
  - Neo4j 節點: 45
  - Neo4j 關係: 23
  - ChromaDB 文檔: 45
  - 處理時間: 2分15秒

✅ 匯入完成!

💡 提示: 資料已匯入資料庫，可以在OpenWebUI中立即使用
```

---

## 🎯 後續步驟

### 1. 在 OpenWebUI 中使用

訪問: http://localhost:8080

選擇使用 Knowledge Base 或 RAG 功能，系統會自動從 Neo4j 和 ChromaDB 檢索相關資訊。

### 2. 建立專用 Knowledge Base

可以在 OpenWebUI 中建立專門的「南藝大知識庫」：

1. 進入 **Workspace** > **Knowledge**
2. 建立新的 Knowledge Base: 「南藝大及漢寶德資料」
3. 系統會自動關聯已匯入的資料

### 3. 持續更新資料

當有新資料時，可以：

```bash
# 方法 1: 匯入新檔案
python3 import_local_data.py -f new_file.pdf --chromadb-port 8000

# 方法 2: 重新匯入整個目錄（會更新現有資料）
python3 import_local_data.py -d "./南藝大及漢寶德校長資料整理" --chromadb-port 8000
```

---

## 📝 資料格式建議

如果你想新增更多資料，建議使用以下格式：

### JSON 格式範例

```json
[
  {
    "title": "漢寶德建築作品：洛韶山莊",
    "artist": "漢寶德",
    "date": "1970",
    "description": "位於台灣中部橫貫公路，展現現代建築與自然環境的融合",
    "category": "建築設計",
    "location": "台灣中橫公路",
    "period": "現代建築"
  }
]
```

### Markdown 格式範例

```markdown
# 漢寶德建築作品：洛韶山莊

## 基本資訊
- **建築師**: 漢寶德
- **年份**: 1970
- **地點**: 台灣中橫公路
- **類型**: 建築設計

## 描述
洛韶山莊是漢寶德早期代表作之一...
```

---

## 🔗 相關資源

- **Neo4j Browser**: http://localhost:7474
- **ChromaDB**: http://localhost:8000
- **OpenWebUI**: http://localhost:8080
- **CLI 工具原始碼**: `import_local_data.py`
- **匯入模組**: `src/importer/`

---

## ✅ 總結

### 系統完全正常，唯一需要注意：

**使用正確的 ChromaDB 端口**: `--chromadb-port 8000`

### 推薦使用方式：

```bash
# 最簡單的方法
bash import_tainan_data.sh

# 或手動執行
python3 import_local_data.py -d "./南藝大及漢寶德校長資料整理" --chromadb-port 8000 -v
```

---

**最後更新**: 2025-12-02
**狀態**: ✅ 系統正常，可以開始匯入
