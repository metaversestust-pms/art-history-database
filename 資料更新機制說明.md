# 🔄 藝術史資料庫 - 資料更新機制說明

**版本**: v1.0
**更新日期**: 2025-11-05
**問題**: 爬蟲更新資料後，OpenWebUI 中的 LLM+RAG 模型會自動更新嗎？

---

## 📊 快速答案

### ✅ **會自動更新！無需手動操作**

當您使用爬蟲系統更新藝術史資料後，所有 OpenWebUI 中的 LLM+RAG 模型組合**都會立即自動使用最新資料**，無需重新創建模型。

---

## 🔍 工作原理詳解

### 系統架構圖

```
爬蟲更新資料
    ↓
【資料層】
├─ Neo4j 資料庫 (圖譜資料)
│   └─ 藝術家、作品、關係
│
└─ ChromaDB 向量資料庫 (嵌入向量)
    └─ 藝術品描述的向量表示
    ↓
【中間層】
Multi-Database RAG Server (Port 8010)
├─ 實時查詢 Neo4j
├─ 實時查詢 ChromaDB
└─ 根據 RAG 策略路由
    ↓
【模型層】
Ollama LLM+RAG 組合模型
├─ llama31-enhanced-rag
├─ qwen3-30b-vector-rag
├─ deepseek-32b-graph-rag
└─ ... (67 個模型)
    ↓
【應用層】
OpenWebUI (Port 8080)
└─ 使用者提問 → 即時檢索最新資料
```

### 關鍵機制

#### 1. **模型只儲存「提示詞」和「參數」**

當您用 `create_full_rag_models.sh` 創建模型時：

```bash
# 創建的 Modelfile 內容
FROM llama3.1:8b

SYSTEM """你是一位專業的藝術史學者...
RAG策略: enhanced_rag
"""

PARAMETER temperature 0.7
PARAMETER num_ctx 4096
```

**模型裡只有**：
- ✅ 基礎 LLM（如 llama3.1:8b）
- ✅ 系統提示詞（角色定義）
- ✅ RAG 策略標識（enhanced_rag, vector_only 等）
- ❌ **不包含任何藝術史資料**

#### 2. **資料儲存在獨立資料庫**

所有藝術史資料儲存在：

**Neo4j 資料庫** (Port 7474)
```
位置: Docker 容器 art-history-neo4j
資料: 藝術家節點、作品節點、關係邊
更新: 運行爬蟲自動更新
持久化: 掛載到本地目錄
```

**ChromaDB 資料庫** (Port 8001)
```
位置: Docker 容器 art-history-chromadb
資料: 藝術品描述的嵌入向量
更新: 運行爬蟲自動更新
持久化: 掛載到本地目錄
```

#### 3. **RAG Server 實時查詢**

Multi-Database RAG Server (Port 8010) 的工作流程：

```javascript
// 1. 接收使用者問題
POST /query
{
  "query": "莫內的睡蓮系列特色？",
  "strategy": "enhanced_rag"
}

// 2. 根據策略決定查詢哪個資料庫
if (strategy === 'enhanced_rag') {
  // 同時查詢 Neo4j 和 ChromaDB
  neo4jResults = queryNeo4j(query);      // 實時查詢
  chromaResults = queryChromaDB(query);  // 實時查詢
}

// 3. 返回檢索結果
return {
  results: [...neo4jResults, ...chromaResults],
  sources: ['Neo4j', 'ChromaDB']
}
```

**關鍵點**：每次查詢都是**實時**從資料庫讀取，而不是從模型中讀取。

---

## 🔄 資料更新流程

### 完整更新流程

```bash
# 步驟 1: 運行爬蟲收集新資料
cd art-history-database
python3 crawl_renaissance_baroque.py

# 步驟 2: 爬蟲自動更新資料庫
→ 新資料寫入 Neo4j
→ 新向量寫入 ChromaDB

# 步驟 3: 在 OpenWebUI 提問
→ 選擇任何 LLM+RAG 模型
→ 提問藝術史問題

# 步驟 4: 系統自動使用最新資料
→ RAG Server 即時查詢資料庫
→ 獲取最新的藝術史資料
→ LLM 基於最新資料生成回答
```

### 時序圖

```
時間軸
─────────────────────────────────────────────────
T0: 初始狀態
    Neo4j: 1000 件作品
    ChromaDB: 1000 個向量
    OpenWebUI: 正常使用

T1: 運行爬蟲 (2025-01-15 10:00)
    $ python3 crawl_renaissance_baroque.py

    爬取 500 件新作品...
    ↓

T2: 資料庫自動更新 (2025-01-15 10:30)
    Neo4j: 1500 件作品 ✅
    ChromaDB: 1500 個向量 ✅

T3: 使用者在 OpenWebUI 提問 (2025-01-15 10:31)
    使用者: "最近添加的文藝復興作品有哪些？"

    llama31-enhanced-rag
    ↓ 呼叫 RAG Server
    ↓ 查詢 Neo4j (實時讀取)
    ↓ 返回包含新作品的結果 ✅
    ↓ LLM 生成回答

T4: 回答包含最新資料
    "根據資料庫，最近添加了以下文藝復興作品：
     1. 拉斐爾的《雅典學院》...
     2. 達文西的《最後的晚餐》..."
    ✅ 自動使用了剛剛爬取的新資料
```

---

## 🎯 關鍵要點

### ✅ 自動更新的原因

1. **資料與模型分離**
   - 模型只是「查詢介面」
   - 資料存在獨立資料庫
   - 查詢時實時讀取

2. **RAG Server 實時查詢**
   - 每次問答都重新查詢資料庫
   - 不依賴緩存或預載資料
   - 確保使用最新資料

3. **資料庫持久化**
   - Neo4j 和 ChromaDB 獨立運行
   - 資料更新即刻生效
   - 不需要重啟服務

### ❌ 不需要做的事

1. ❌ **不需要重新創建 Ollama 模型**
   ```bash
   # 錯誤做法：每次更新資料後重新運行
   # bash create_full_rag_models.sh
   ```

2. ❌ **不需要重啟 OpenWebUI**
   ```bash
   # 錯誤做法：重啟容器
   # docker restart art-history-openwebui
   ```

3. ❌ **不需要清除緩存**
   ```bash
   # 錯誤做法：清除向量索引
   # 資料庫會自動使用最新資料
   ```

---

## 🧪 驗證方法

### 測試數據更新是否生效

**步驟 1: 記錄當前資料量**
```bash
# 查詢 Neo4j 節點數
docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n) RETURN count(n) as total_nodes"

# 輸出: total_nodes: 4946
```

**步驟 2: 運行爬蟲添加新資料**
```bash
python3 crawl_renaissance_baroque.py
# 爬取 100 件新作品
```

**步驟 3: 確認資料已增加**
```bash
docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n) RETURN count(n) as total_nodes"

# 輸出: total_nodes: 5046 ✅ (+100)
```

**步驟 4: 在 OpenWebUI 提問測試**
```
問題: "資料庫中有多少件藝術品？"

預期回答: 應該反映最新數量（5046）
```

**步驟 5: 測試新資料可檢索**
```
問題: "最近添加的文藝復興作品有哪些？"

預期: ✅ 能找到剛剛爬取的新作品
```

---

## 🔧 技術細節

### RAG 查詢流程

```javascript
// multi-database-rag-server.js

// 每次查詢都執行以下步驟
async function handleQuery(query, strategy) {
  // 1. 生成查詢向量（即時）
  const embedding = await generateEmbedding(query);

  // 2. 查詢資料庫（即時）
  if (strategy === 'vector_only') {
    results = await queryChromaDB(query, embedding);
  } else if (strategy === 'graph_only') {
    results = await queryNeo4j(query);
  } else {
    // enhanced_rag: 同時查詢兩個資料庫
    const chromaResults = await queryChromaDB(query, embedding);
    const neo4jResults = await queryNeo4j(query);
    results = [...chromaResults, ...neo4jResults];
  }

  // 3. 返回最新結果
  return results;
}
```

### 資料庫連接配置

```javascript
// 配置位置: multi-database-rag-server.js

// Neo4j
this.neo4jUrl = 'http://localhost:7474';
this.neo4jAuth = Buffer.from('neo4j:arthistory123').toString('base64');

// ChromaDB
this.chromaUrl = 'http://localhost:8001';
this.chromaCollectionId = 'aa7a55a2-924e-41b0-a0f7-9b5c031477a4';
```

這些連接在服務器啟動時建立，每次查詢都使用**最新的資料庫內容**。

---

## 📋 常見問題

### Q1: 更新資料後需要等多久才能在 OpenWebUI 中看到？

**A**: **立即生效**，0 等待時間。

資料寫入 Neo4j 或 ChromaDB 後，下一次查詢就會使用新資料。

### Q2: 如果我同時運行多個爬蟲，會有衝突嗎？

**A**: **不會衝突**，但建議序列執行。

Neo4j 和 ChromaDB 都支持並發寫入，但為了資料一致性，建議一次運行一個爬蟲。

### Q3: 資料會不會佔用 Ollama 模型的空間？

**A**: **不會**。

Ollama 模型只儲存 LLM 權重和提示詞，資料完全獨立存儲在 Neo4j 和 ChromaDB 中。

### Q4: 如果刪除資料，OpenWebUI 會立即反映嗎？

**A**: **是的**，刪除也是即時生效。

```bash
# 刪除 Neo4j 中的某個作品
docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (a:Artwork {objectID: '12345'}) DETACH DELETE a"

# 下一次查詢就找不到這個作品了
```

### Q5: 我可以手動清空資料庫重新開始嗎？

**A**: 可以，但需要重新運行爬蟲。

```bash
# 清空 Neo4j（謹慎操作！）
docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n) DETACH DELETE n"

# 清空後重新爬取
python3 crawl_renaissance_baroque.py
```

### Q6: 向量索引需要重建嗎？

**A**: **不需要**。

ChromaDB 和 Neo4j 的向量索引會自動維護，添加新資料時會自動索引。

---

## 🎯 最佳實踐

### ✅ 推薦做法

1. **定期更新資料**
   ```bash
   # 設置定時任務（每週更新）
   crontab -e
   # 添加: 0 2 * * 0 cd /path/to/project && python3 crawl_renaissance_baroque.py
   ```

2. **監控資料量變化**
   ```bash
   # 創建簡單的監控腳本
   ./scripts/check_database_stats.sh
   ```

3. **測試新資料可用性**
   ```
   更新後在 OpenWebUI 提問驗證
   ```

### ❌ 避免做法

1. ❌ 更新資料後重新創建所有模型（浪費時間）
2. ❌ 頻繁重啟 Docker 容器（不必要）
3. ❌ 手動修改資料庫連接配置（除非遷移）

---

## 🔄 資料更新腳本示例

### 完整的資料更新工作流

```bash
#!/bin/bash
# 文件: update_art_history_data.sh

echo "🎨 開始更新藝術史資料..."
echo "================================"

# 1. 檢查服務狀態
echo "📊 檢查資料庫服務..."
docker ps | grep -E "neo4j|chromadb|rag-server"

# 2. 記錄更新前的資料量
echo "📈 更新前資料統計:"
NEO4J_BEFORE=$(docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n) RETURN count(n)" --format plain | tail -n 1)
echo "  Neo4j 節點數: $NEO4J_BEFORE"

# 3. 運行爬蟲
echo "🕷️ 開始爬取新資料..."
python3 crawl_renaissance_baroque.py

# 4. 確認資料已更新
echo "📈 更新後資料統計:"
NEO4J_AFTER=$(docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n) RETURN count(n)" --format plain | tail -n 1)
echo "  Neo4j 節點數: $NEO4J_AFTER"

# 5. 計算增量
DIFF=$((NEO4J_AFTER - NEO4J_BEFORE))
echo "✅ 新增 $DIFF 個節點"

# 6. 測試提示
echo ""
echo "🎉 資料更新完成！"
echo "💡 現在可以在 OpenWebUI (http://localhost:8080) 中測試新資料"
echo ""
echo "建議測試問題:"
echo "  - 資料庫中有多少件藝術品？"
echo "  - 最近添加的作品有哪些？"
```

### 使用方法

```bash
# 賦予執行權限
chmod +x update_art_history_data.sh

# 運行更新
./update_art_history_data.sh
```

---

## 📊 系統狀態檢查腳本

```bash
#!/bin/bash
# 文件: check_system_status.sh

echo "🔍 藝術史資料庫系統狀態"
echo "================================"

# Neo4j
echo "📊 Neo4j 資料庫:"
docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n) RETURN labels(n)[0] as type, count(n) as count ORDER BY type" \
  --format plain

# ChromaDB
echo ""
echo "📊 ChromaDB 資料庫:"
CHROMA_COUNT=$(curl -s http://localhost:8001/api/v2/tenants/default_tenant/databases/default_database/collections/aa7a55a2-924e-41b0-a0f7-9b5c031477a4 | jq '.count')
echo "  向量數量: $CHROMA_COUNT"

# RAG Server
echo ""
echo "📊 RAG Server 狀態:"
curl -s http://localhost:8010/health | jq '.'

# Ollama 模型
echo ""
echo "📊 Ollama 模型數量:"
MODEL_COUNT=$(docker exec art-history-ollama ollama list | grep -c "rag")
echo "  RAG 組合模型: $MODEL_COUNT 個"

echo ""
echo "✅ 系統檢查完成"
```

---

## 🎉 總結

### 關鍵結論

| 問題 | 答案 | 說明 |
|-----|------|------|
| 資料會自動更新嗎？ | ✅ 是 | 實時查詢資料庫 |
| 需要重新創建模型嗎？ | ❌ 否 | 模型不含資料 |
| 需要重啟服務嗎？ | ❌ 否 | 即時生效 |
| 更新後多久可用？ | ⚡ 立即 | 0 等待時間 |

### 原理精髓

```
模型 = LLM + 提示詞 + RAG策略標識
資料 = Neo4j + ChromaDB（獨立存儲）
查詢 = 實時讀取 → 永遠是最新資料
```

### 你只需要做

1. ✅ 運行爬蟲收集新資料
2. ✅ 在 OpenWebUI 中正常使用
3. ✅ 系統自動使用最新資料

就這麼簡單！🎨✨

---

## 📚 相關文件

- `crawl_renaissance_baroque.py` - 藝術史爬蟲
- `multi-database-rag-server.js` - RAG 查詢服務器
- `create_full_rag_models.sh` - 模型創建腳本（一次性）
- `docker-compose.yml` - 資料庫容器配置

---

**最後更新**: 2025-11-05
**維護者**: Art History Database Team
