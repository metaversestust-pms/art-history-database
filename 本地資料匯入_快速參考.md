# 📋 本地資料匯入 - 快速參考卡

## 🚀 快速開始

```bash
# 1. 設置環境 (首次使用)
chmod +x setup_importer.sh
./setup_importer.sh

# Windows用戶
setup_importer.bat

# 2. 建立範例模板
python import_local_data.py --create-template

# 3. 匯入資料
python import_local_data.py -f my_artwork.json
```

---

## 📁 支援格式

| 格式 | 副檔名 | 說明 |
|-----|--------|------|
| JSON | `.json`, `.jsonl` | 結構化資料 |
| PDF | `.pdf` | 自動提取文本 |
| 文字檔 | `.txt`, `.md` | 純文本資料 |

---

## 💻 CLI命令

### 基本操作

```bash
# 匯入單一檔案
python import_local_data.py -f 檔案.json

# 匯入整個目錄
python import_local_data.py -d ./資料夾/

# 遞迴處理子目錄
python import_local_data.py -d ./資料夾/ --recursive
```

### 選擇儲存位置

```bash
# 只存Neo4j
python import_local_data.py -f file.json --neo4j-only

# 只存ChromaDB
python import_local_data.py -f file.json --chromadb-only

# 不儲存(僅測試)
python import_local_data.py -f file.json --no-save
```

### 顯示詳細資訊

```bash
python import_local_data.py -f file.json --verbose
```

---

## 🌐 Web介面

### 啟動

```bash
python web_import_server.py
```

### 訪問

```
http://localhost:5050
```

### 操作

1. 拖放檔案或點擊選擇
2. 選擇儲存選項
3. 點擊「開始匯入」

---

## 📝 資料格式範例

### JSON格式

```json
{
  "artworks": [
    {
      "title": "蒙娜麗莎",
      "artist": "Leonardo da Vinci",
      "date": "1503-1519",
      "period": "文藝復興",
      "description": "最著名的肖像畫..."
    }
  ]
}
```

### 文字格式

```
標題: 蒙娜麗莎
藝術家: Leonardo da Vinci
日期: 1503-1519
時期: 文藝復興
描述: 最著名的肖像畫...
```

### Markdown格式

```markdown
# 蒙娜麗莎

作者: Leonardo da Vinci
年代: 1503-1519

最著名的肖像畫...
```

---

## 🔍 驗證匯入

### 查詢Neo4j

```bash
docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n:Artwork) WHERE n.source = 'local_import' RETURN count(n)"
```

### 在OpenWebUI測試

```
提問: "資料庫中有哪些本地匯入的作品?"
```

---

## ⚡ 常用欄位

| 欄位 | 說明 | 必填 |
|-----|------|-----|
| `title` | 作品標題 | ✅ |
| `artist` | 藝術家 | - |
| `date` | 創作日期 | - |
| `period` | 藝術時期 | - |
| `description` | 描述 | - |
| `style` | 風格 | - |
| `medium` | 媒材 | - |
| `location` | 地點 | - |

---

## 🐛 故障排除

### 問題1: 找不到neo4j模組

```bash
pip install neo4j chromadb requests
```

### 問題2: PDF無法處理

```bash
pip install PyPDF2 pdfplumber
```

### 問題3: 資料庫連接失敗

```bash
# 檢查服務
docker ps | grep -E "neo4j|chromadb"

# 啟動服務
docker-compose up -d
```

### 問題4: Web介面無法訪問

```bash
# 檢查端口占用
lsof -i :5050

# 更換端口 (編輯 web_import_server.py)
app.run(port=5051)
```

---

## 📂 檔案結構

```
art-history-database/
├── src/importer/              # 匯入模組
│   ├── pdf_processor.py      # PDF處理
│   ├── json_processor.py     # JSON處理
│   ├── text_processor.py     # 文字處理
│   └── importer.py           # 主要匯入器
├── import_local_data.py      # CLI工具
├── web_import_server.py      # Web介面
├── setup_importer.sh         # 設置腳本(Linux/Mac)
├── setup_importer.bat        # 設置腳本(Windows)
├── 本地資料匯入完整指南.md   # 完整文檔
└── import_templates/          # 範例模板
    ├── template.json
    ├── template_structured.txt
    ├── template_markdown.md
    └── template_separated.txt
```

---

## 🔗 相關資源

| 資源 | 說明 |
|-----|------|
| `本地資料匯入完整指南.md` | 詳細使用文檔 |
| `import_templates/` | 範例模板檔案 |
| OpenWebUI | http://localhost:8080 |
| Neo4j Browser | http://localhost:7474 |

---

## 📊 完整工作流程

```bash
# 1. 準備資料
mkdir ~/my_artworks
cp my_data.json ~/my_artworks/

# 2. 檢查服務
docker ps | grep neo4j

# 3. 匯入資料
cd art-history-database
python import_local_data.py -d ~/my_artworks/ --recursive

# 4. 驗證
# 在OpenWebUI中測試查詢

# 5. 查看統計
# 查看命令列輸出的統計資訊
```

---

## 💡 提示

- ✅ 只有 `title` 是必填欄位
- ✅ 系統會自動偵測時期和關鍵詞
- ✅ 匯入後立即可在RAG系統中使用
- ✅ 支援批次匯入多個檔案
- ✅ 重複匯入會自動更新而非重複建立

---

**版本**: v1.0 | **更新**: 2025-01-30
