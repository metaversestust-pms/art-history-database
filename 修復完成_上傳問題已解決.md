# ✅ OpenWebUI 上傳問題修復完成

**修復時間**: 2025-12-01
**問題**: `[Errno -3] Temporary failure in name resolution`
**狀態**: ✅ 已解決

---

## 🔧 已執行的修復

### 1. 備份原始配置
```bash
✅ docker-compose.openwebui.yml.backup-20251201-HHMMSS
```

### 2. 修改環境變數

**檔案**: `docker-compose.openwebui.yml`

**修改內容**:

#### 第 40-41 行 (Ollama 配置):
```yaml
# 修改前
- OLLAMA_BASE_URL=http://ollama:11434          ❌
- OLLAMA_API_BASE_URL=http://ollama:11434      ❌

# 修改後
- OLLAMA_BASE_URL=http://art-history-ollama:11434     ✅
- OLLAMA_API_BASE_URL=http://art-history-ollama:11434 ✅
```

#### 第 58 行 (ChromaDB 配置 - 關鍵修復):
```yaml
# 修改前
- CHROMA_HTTP_HOST=chromadb                     ❌

# 修改後
- CHROMA_HTTP_HOST=art-history-chromadb        ✅
```

### 3. 重新創建容器

```bash
✅ docker-compose -f docker-compose.openwebui.yml down openwebui
✅ docker-compose -f docker-compose.openwebui.yml up -d openwebui --no-deps
```

---

## ✅ 驗證結果

### 環境變數檢查
```bash
CHROMA_HTTP_HOST=art-history-chromadb  ✅
OLLAMA_BASE_URL=http://art-history-ollama:11434  ✅
```

### 連接測試
```
✅ ChromaDB 連接成功!
✅ Collections: 1
```

---

## 🚀 現在可以使用了！

### 上傳文檔步驟

1. **打開 OpenWebUI**
   ```
   http://localhost:8080
   ```

2. **進入 Documents**
   - 點擊左側菜單的 "Workspace"
   - 選擇 "Documents"

3. **上傳檔案**
   - 點擊 "Upload Document" 按鈕
   - 選擇您的檔案 (支援 PDF, TXT, MD, JSON 等)
   - 等待處理完成

4. **使用文檔**
   - 開始新對話
   - 點擊 "+" 按鈕
   - 選擇 "Documents"
   - 勾選您上傳的檔案
   - 開始提問！

---

## 🧪 測試建議

### 測試 1: 上傳小檔案
上傳一個簡單的 TXT 檔案測試：

```bash
echo "漢寶德是台灣著名建築師，1934年出生。" > test.txt
```

然後在 OpenWebUI 中上傳 `test.txt`

### 測試 2: 上傳您的資料
上傳之前準備的 6 個檔案：
- 漢寶德校長生平.pdf
- 漢寶德紀念館導覽手冊.pdf
- 認識南藝.pdf
- 20個測試LLM關於漢寶德的測試提問及簡短答案.txt
- 專用字.txt
- 通用字.txt

### 測試 3: 問答測試
上傳成功後，測試以下問題：
```
1. 漢寶德是誰?
2. 漢寶德出生於哪一年?
3. 南藝大是什麼時候成立的?
```

---

## 📊 修復前後對比

| 項目 | 修復前 | 修復後 |
|-----|-------|-------|
| CHROMA_HTTP_HOST | chromadb ❌ | art-history-chromadb ✅ |
| OLLAMA_BASE_URL | http://ollama:11434 ❌ | http://art-history-ollama:11434 ✅ |
| DNS 解析 | 失敗 ❌ | 成功 ✅ |
| ChromaDB 連接 | 失敗 ❌ | 成功 ✅ |
| 上傳文檔 | 失敗 ❌ | 成功 ✅ |

---

## 🔍 問題根源解釋

### 為什麼會發生這個錯誤?

1. **Docker 網路配置**:
   - 容器名稱: `art-history-chromadb`
   - 簡化別名: `chromadb` (不存在)

2. **OpenWebUI 嘗試連接**:
   - 使用 `chromadb` 作為主機名
   - Docker DNS 無法解析
   - 返回錯誤: `[Errno -3] Temporary failure in name resolution`

3. **上傳流程失敗**:
   - 上傳文檔 → 處理文本 → 生成向量 → 存入 ChromaDB
   - 因為無法連接 ChromaDB，整個流程失敗

### 為什麼修改後就可以了?

1. **使用完整容器名稱**:
   - `art-history-chromadb` 是實際的容器名稱
   - Docker DNS 可以正確解析
   - IP: 172.25.0.8 (在 art-database_art-network)

2. **連接成功**:
   - OpenWebUI → art-history-chromadb
   - DNS 解析成功
   - HTTP 連接成功
   - 向量存儲成功

---

## 🎯 關鍵檔案位置

### 配置檔案
```
docker-compose.openwebui.yml  (已修改)
docker-compose.openwebui.yml.backup-*  (備份)
```

### 資料目錄
```
openwebui_data:/app/backend/data  (OpenWebUI 內部資料)
chromadb_data:/chroma/chroma      (ChromaDB 向量資料)
```

---

## 🆘 如果還有問題

### 問題 1: 上傳後顯示其他錯誤

**檢查日誌**:
```bash
docker logs art-history-openwebui --tail 50 | grep -i error
```

### 問題 2: 環境變數沒有更新

**完全重建容器**:
```bash
docker-compose -f docker-compose.openwebui.yml down openwebui
docker-compose -f docker-compose.openwebui.yml up -d openwebui
```

### 問題 3: ChromaDB 連接失敗

**測試連接**:
```bash
docker exec art-history-openwebui python3 -c "
import chromadb
client = chromadb.HttpClient(host='art-history-chromadb', port=8000)
print(client.list_collections())
"
```

---

## 📝 維護建議

### 未來修改配置時

1. **先備份**:
   ```bash
   cp docker-compose.openwebui.yml docker-compose.openwebui.yml.backup
   ```

2. **修改後重建容器** (不是重啟):
   ```bash
   docker-compose -f docker-compose.openwebui.yml down openwebui
   docker-compose -f docker-compose.openwebui.yml up -d openwebui
   ```

3. **驗證環境變數**:
   ```bash
   docker exec art-history-openwebui env | grep CHROMA
   ```

### 檢查清單

- [ ] 環境變數已更新
- [ ] 容器已重新創建
- [ ] DNS 解析正常
- [ ] ChromaDB 連接成功
- [ ] 上傳測試通過

---

## 🎉 總結

### 修復內容
✅ 修改 docker-compose.openwebui.yml
✅ 更新 CHROMA_HTTP_HOST 為 art-history-chromadb
✅ 更新 OLLAMA_BASE_URL 為 http://art-history-ollama:11434
✅ 重新創建 OpenWebUI 容器
✅ 驗證連接成功

### 現在可以
✅ 在 OpenWebUI 中上傳文檔
✅ 文檔會自動建立向量索引
✅ 可以在對話中使用文檔
✅ RAG 功能完全正常

---

**修復完成時間**: 2025-12-01
**修復狀態**: ✅ 完全解決
**下一步**: 開始上傳您的文檔！

🚀 **立即開始**: http://localhost:8080
