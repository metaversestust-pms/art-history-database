# 🤖 自動化網路爬蟲系統 - 完整說明

## 📋 系統總覽

已成功建立**全自動藝術史資料收集系統**，能夠：

✅ **自動收集**：從 Harvard Art Museums 和 Met Museum 收集資料
✅ **智能處理**：自動清洗、標準化、去重
✅ **即時同步**：收集完立即傳送到 Neo4j 和 ChromaDB
✅ **排程執行**：支援每24小時自動執行
✅ **重點收集**：優先收集文藝復興和巴洛克時期資料

---

## 🎯 核心功能

### 1. 多來源資料收集

**Harvard Art Museums API**
- 世界級博物館藏品
- 完整的元資料
- 高品質圖片資源

**Metropolitan Museum API**
- 超過 40 萬件藏品
- 開放資料政策
- 免費使用

### 2. 智能爬取策略

#### 依藝術家爬取
**文藝復興藝術家：**
- Leonardo da Vinci（達文西）
- Michelangelo（米開朗基羅）
- Raphael（拉斐爾）
- Botticelli（波提切利）
- Titian（提香）
- Donatello（多納泰羅）
- 等13位重要藝術家

**巴洛克藝術家：**
- Caravaggio（卡拉瓦喬）
- Rembrandt（林布蘭）
- Rubens（魯本斯）
- Velázquez（委拉斯貴茲）
- Vermeer（維梅爾）
- Bernini（貝尼尼）
- 等12位重要藝術家

#### 依關鍵詞爬取
- Renaissance, High Renaissance, Italian Renaissance
- Baroque, Caravaggism, Dutch Golden Age
- 等藝術史專業術語

#### 依時期爬取
- 文藝復興：1400-1600
- 巴洛克：1600-1750

### 3. 資料處理管道

```
原始資料 → 標準化 → 去重 → 驗證 → 增強 → 傳送到RAG
```

**標準化處理：**
- 統一資料格式
- 提取關鍵資訊
- 標準化日期格式

**去重處理：**
- 基於作品ID去重
- 支援跨資料源去重

**驗證處理：**
- 驗證必要欄位
- 過濾無效資料

### 4. RAG 系統整合

**Neo4j 圖資料庫**
```cypher
MERGE (a:Artwork {id: $id})
SET a.title = $title,
    a.artist = $artist,
    a.period = $period,
    a.date = $date
```

**ChromaDB 向量資料庫**
```python
collection.add(
    ids=[artwork_id],
    documents=[full_text],
    metadatas=[metadata]
)
```

**即時同步機制：**
- 爬取完立即處理
- 批次傳送到資料庫
- 失敗自動重試

---

## 📁 檔案結構

```
art-history-database/
│
├── src/
│   └── crawler/
│       └── AutomatedCrawlerSystem.py     # 主系統程式
│
├── crawler_config.yaml                    # 配置檔案
├── docker-compose.crawler.yml             # Docker 服務配置
│
├── start_automated_crawler.sh             # 啟動腳本
├── 快速啟動爬蟲.md                         # 快速指南
├── 啟動自動化爬蟲_簡易版.md                # 簡易指南
├── 自動化爬蟲系統使用指南.md               # 完整指南
│
├── automated_crawler_data/                # 輸出資料目錄
│   ├── renaissance_*.json                 # 文藝復興資料
│   ├── baroque_*.json                     # 巴洛克資料
│   └── crawler_stats.json                 # 統計資料
│
└── logs/
    └── automated_crawler.log              # 系統日誌
```

---

## 🚀 快速開始（3步驟）

### 步驟 1：啟動 RAG 服務

```bash
cd art-history-database
docker-compose -f docker-compose.crawler.yml up -d neo4j chromadb
```

### 步驟 2：修改配置

編輯 `crawler_config.yaml`：
```yaml
rag_system:
  neo4j:
    password: "your_password"  # 改成你的密碼
```

### 步驟 3：啟動爬蟲

```bash
# 安裝依賴（首次）
pip3 install requests schedule pyyaml neo4j chromadb

# 啟動爬蟲
python3 src/crawler/AutomatedCrawlerSystem.py

# 選擇模式：
# 1 = 單次執行
# 2 = 自動排程
```

---

## ⚙️ 配置說明

### crawler_config.yaml 核心配置

```yaml
# 重點收集時期
crawler:
  focus_periods:
    - renaissance      # 文藝復興
    - baroque         # 巴洛克

# 爬取策略
  strategy:
    max_results_per_artist: 30   # 每位藝術家收集30件作品
    max_results_per_keyword: 20  # 每個關鍵詞收集20件作品

# 排程設定
schedule:
  interval_hours: 24             # 每24小時執行一次

# RAG系統連接
rag_system:
  neo4j:
    url: "bolt://localhost:7687"
    user: "neo4j"
    password: "your_password"    # ⚠️ 修改這裡

  chromadb:
    host: "localhost"
    port: 8000
```

---

## 📊 預期結果

### 第一次執行

**收集數量：**
- 文藝復興：200-500 件作品
- 巴洛克：200-500 件作品
- 總計：400-1000 件作品

**執行時間：**
- 約 15-30 分鐘（取決於網路速度）

**資料內容：**
```json
{
  "id": "harvard_299843",
  "source": "harvard",
  "title": "Mona Lisa",
  "artist": "Leonardo da Vinci",
  "date": "1503-1519",
  "period": "Renaissance",
  "culture": "Italian",
  "medium": "Oil on wood",
  "description": "Portrait of Lisa Gherardini...",
  "image_url": "https://..."
}
```

### 持續執行

**每次排程執行：**
- 持續收集新資料
- 自動去除重複
- 擴充資料庫內容
- 提高查詢準確度

---

## 🔍 資料驗證

### 方法 1：查看本地檔案

```bash
# 查看收集的資料
ls -lh automated_crawler_data/

# 查看統計
cat automated_crawler_data/crawler_stats.json
```

### 方法 2：查詢 Neo4j

```bash
# 打開 Neo4j Browser
http://localhost:7474

# 執行查詢
MATCH (a:Artwork)
WHERE a.period IN ['Renaissance', 'Baroque']
RETURN a.title, a.artist, a.period
LIMIT 20
```

### 方法 3：查詢 ChromaDB

```python
import chromadb

client = chromadb.HttpClient(host='localhost', port=8000)
collection = client.get_collection("art_history")

# 查看總數
print(f"總計: {collection.count()} 件作品")

# 搜索測試
results = collection.query(
    query_texts=["Leonardo da Vinci paintings"],
    n_results=5
)
print(results)
```

### 方法 4：OpenWebUI 測試

```
打開：http://localhost:8080

測試查詢：
- "文藝復興時期有哪些著名的藝術家？"
- "達文西的蒙娜麗莎是什麼時候創作的？"
- "巴洛克藝術和文藝復興藝術有什麼不同？"
- "卡拉瓦喬使用了什麼特殊的繪畫技法？"
```

---

## 📈 監控與維護

### 即時監控

```bash
# 查看即時日誌
tail -f logs/automated_crawler.log

# 查看錯誤
grep ERROR logs/automated_crawler.log
```

### 統計資訊

```bash
python3 -c "
import json
with open('automated_crawler_data/crawler_stats.json') as f:
    stats = json.load(f)
    print('爬蟲統計：')
    print(f'  總計爬取: {stats[\"total_crawled\"]}')
    print(f'  總計處理: {stats[\"total_processed\"]}')
    print(f'  傳送到RAG: {stats[\"total_sent_to_rag\"]}')
    print(f'  最後執行: {stats[\"last_crawl_time\"]}')
"
```

### 定期維護

**每週：**
- 檢查日誌檔案
- 驗證資料品質
- 清理舊日誌

**每月：**
- 分析收集效果
- 調整爬取策略
- 更新藝術家列表

---

## 🎨 擴展功能

### 添加新時期

編輯 `src/crawler/AutomatedCrawlerSystem.py`，在 `ArtPeriodDefinitions.PERIODS` 中添加：

```python
"impressionism": {
    "name": "印象派",
    "display_name": "Impressionism",
    "date_range": (1860, 1900),
    "keywords": ["Impressionism", "Post-Impressionism"],
    "artists": ["Monet", "Renoir", "Degas", "Cézanne"],
    "priority": 2
}
```

然後在 `crawler_config.yaml` 啟用：

```yaml
crawler:
  focus_periods:
    - renaissance
    - baroque
    - impressionism  # 新增
```

### 添加新資料源

在 `src/crawler/AutomatedCrawlerSystem.py` 中添加新的爬蟲類：

```python
class NewMuseumCrawler:
    def __init__(self, api_key):
        self.api_key = api_key
        # ... 實現爬蟲邏輯
```

### 自定義處理邏輯

修改 `DataProcessor` 類的方法：

```python
def custom_enrichment(self, artwork: Dict) -> Dict:
    # 添加自定義的資料增強邏輯
    return artwork
```

---

## 🐛 故障排除

### 常見問題

**Q1: 找不到 Python 模組**
```bash
pip3 install requests schedule pyyaml neo4j chromadb
```

**Q2: 無法連接 Neo4j**
```bash
# 檢查服務
docker ps | grep neo4j

# 檢查配置
grep password crawler_config.yaml

# 重啟服務
docker-compose -f docker-compose.crawler.yml restart neo4j
```

**Q3: ChromaDB 連接失敗**
```bash
# 測試連接
curl http://localhost:8000/api/v1/heartbeat

# 重啟服務
docker-compose -f docker-compose.crawler.yml restart chromadb
```

**Q4: API 請求失敗**
```bash
# 檢查網路
ping api.harvardartmuseums.org

# 檢查日誌
grep "API" logs/automated_crawler.log
```

**Q5: 爬取速度慢**
```yaml
# 調整配置
crawler:
  strategy:
    delay_between_requests: 0.3  # 減少延遲（注意API限制）
```

---

## 💡 最佳實踐

### 1. 首次使用

1. 先執行**單次測試**（模式1）
2. 驗證資料正確性
3. 調整配置參數
4. 再啟動**自動排程**（模式2）

### 2. 生產環境

```yaml
# 推薦配置
crawler:
  strategy:
    max_results_per_artist: 50
    max_results_per_keyword: 30
    delay_between_requests: 0.5

schedule:
  interval_hours: 24
  start_time: "02:00"  # 凌晨執行
```

### 3. 資料品質

- 定期檢查收集的資料
- 驗證 RAG 系統查詢結果
- 根據需求調整過濾條件

### 4. 系統維護

```bash
# 定期清理舊日誌
find logs/ -name "*.log" -mtime +30 -delete

# 定期備份資料
tar -czf backup_$(date +%Y%m%d).tar.gz automated_crawler_data/
```

---

## 📚 技術架構

### 系統組件

```
┌─────────────────────────────────────────┐
│   AutomatedCrawlerSystem (主系統)        │
└─────────────────────────────────────────┘
              │
    ┌─────────┼─────────┐
    │         │         │
    ▼         ▼         ▼
┌─────────┐ ┌─────────┐ ┌─────────┐
│Harvard  │ │  Met    │ │其他來源 │
│Crawler  │ │Crawler  │ │        │
└─────────┘ └─────────┘ └─────────┘
              │
              ▼
    ┌──────────────────┐
    │  DataProcessor   │
    │  - normalize     │
    │  - deduplicate   │
    │  - validate      │
    └──────────────────┘
              │
    ┌─────────┴─────────┐
    │                   │
    ▼                   ▼
┌─────────┐       ┌──────────┐
│Neo4j    │       │ChromaDB  │
│Graph DB │       │Vector DB │
└─────────┘       └──────────┘
```

### 資料流程

```
1. 排程觸發
   ↓
2. 初始化爬蟲
   ↓
3. 依策略爬取
   ├─ 藝術家
   ├─ 關鍵詞
   └─ 時期
   ↓
4. 資料處理
   ├─ 標準化
   ├─ 去重
   └─ 驗證
   ↓
5. 保存本地
   ↓
6. 傳送到RAG
   ├─ Neo4j
   └─ ChromaDB
   ↓
7. 更新統計
   ↓
8. 等待下次排程
```

---

## 🎯 使用場景

### 場景1：研究人員

**需求：** 收集大量藝術史資料用於研究

**配置：**
```yaml
crawler:
  focus_periods:
    - renaissance
    - baroque
    - rococo
  strategy:
    max_results_per_artist: 100
```

### 場景2：教育機構

**需求：** 建立教學資料庫

**配置：**
```yaml
crawler:
  focus_periods:
    - renaissance
    - baroque
processing:
  filters:
    require_image: true
    require_description: true
```

### 場景3：博物館

**需求：** 定期更新館藏資訊

**配置：**
```yaml
schedule:
  interval_hours: 168  # 每週執行
  start_time: "02:00"
```

---

## 🔗 相關資源

- **Harvard Art Museums API**: https://github.com/harvardartmuseums/api-docs
- **Met Museum API**: https://metmuseum.github.io/
- **Neo4j 文檔**: https://neo4j.com/docs/
- **ChromaDB 文檔**: https://docs.trychroma.com/
- **Python Schedule**: https://schedule.readthedocs.io/

---

## 📞 支援

遇到問題？

1. **檢查日誌**: `logs/automated_crawler.log`
2. **查看文檔**: 本目錄下的各種指南
3. **驗證配置**: 執行測試模式
4. **檢查服務**: 確保 Neo4j 和 ChromaDB 運行中

---

## ✅ 總結

您現在擁有一個完整的**自動化藝術史資料收集系統**！

**系統特點：**
- 🤖 全自動運行
- 🎨 重點收集文藝復興和巴洛克
- 🔄 即時同步到 RAG 系統
- 📊 完整的監控和統計
- 🛠️ 靈活的配置選項

**開始使用：**
```bash
cd art-history-database
python3 src/crawler/AutomatedCrawlerSystem.py
```

讓系統為您自動收集和擴充藝術史資料庫吧！🎨
