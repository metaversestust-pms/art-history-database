# 📚 藝術史資料更新 - 快速指南

> **核心答案**：✅ **資料會自動更新，無需任何手動操作！**

---

## 🎯 一句話總結

當您用爬蟲更新藝術史資料後，OpenWebUI 中的所有 67 個 LLM+RAG 模型組合**立即自動使用最新資料**，無需重新創建模型或重啟服務。

---

## 🔄 更新流程（3 步驟）

### 步驟 1: 運行爬蟲

```bash
cd art-history-database
python3 crawl_renaissance_baroque.py
```

### 步驟 2: 等待爬蟲完成

```
🕷️ 正在爬取新資料...
✅ 新增 500 件作品
💾 資料已寫入 Neo4j 和 ChromaDB
```

### 步驟 3: 直接使用

```
打開 http://localhost:8080
選擇任何模型（如 llama31-70b-enhanced-rag）
提問 → 系統自動使用最新資料 ✅
```

---

## 💡 為什麼會自動更新？

### 系統架構

```
【模型層】                【資料層】
Ollama 模型              Neo4j 資料庫
├─ llama31-70b          ├─ 6,773 個節點
├─ qwen3-30b       →    ├─ 6,812 個關係
└─ deepseek-32b         └─ ChromaDB 向量
    ↓                        ↓
    實時查詢             實時更新
```

### 關鍵機制

1. **模型不含資料**
   - 模型只儲存：LLM 權重 + 提示詞 + RAG 策略
   - 不儲存任何藝術史資料

2. **資料獨立存儲**
   - Neo4j: 圖譜資料（藝術家、作品、關係）
   - ChromaDB: 向量資料（嵌入表示）

3. **實時查詢**
   - 每次提問都即時查詢資料庫
   - 永遠使用最新資料

---

## ✅ 驗證方法

### 快速測試

```bash
# 1. 記錄當前資料量
docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n) RETURN count(n)"
# 輸出: 6773

# 2. 運行爬蟲
python3 crawl_renaissance_baroque.py

# 3. 確認資料增加
docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n) RETURN count(n)"
# 輸出: 7273 ✅ (+500)

# 4. 在 OpenWebUI 提問
問題: "資料庫中有多少件藝術品？"
預期: 回答應反映最新數量 (7273)
```

---

## 🚫 不需要做的事

### ❌ 錯誤做法

```bash
# 錯誤 1: 重新創建模型（浪費時間！）
bash create_full_rag_models.sh  # ❌ 不需要

# 錯誤 2: 重啟 Docker 容器（不必要！）
docker restart art-history-openwebui  # ❌ 不需要

# 錯誤 3: 清除緩存（自動使用最新資料！）
# 完全不需要任何清除操作  # ❌ 不需要
```

### ✅ 正確做法

```bash
# 正確：只需運行爬蟲
python3 crawl_renaissance_baroque.py  # ✅

# 然後直接在 OpenWebUI 使用
# 系統自動使用最新資料  # ✅
```

---

## 📊 當前系統狀態

### 系統資訊（從狀態檢查腳本獲取）

```
✅ 所有關鍵服務正常運行 (4/4)

📊 資料庫統計:
   • Neo4j 節點數: 6,773
   • Neo4j 關係數: 6,812
   • 向量索引: 3 個 (全部 ONLINE)

🤖 模型統計:
   • 基礎 LLM 模型: 11 個
   • LLM+RAG 組合: 67 個

🌐 OpenWebUI: http://localhost:8080 (正常)
```

---

## 🛠️ 實用腳本

### 1. 更新資料腳本

```bash
# 運行完整更新流程
bash update_art_history_data.sh

# 功能：
# - 檢查服務狀態
# - 記錄更新前資料量
# - 運行爬蟲
# - 確認資料已更新
# - 提供測試建議
```

### 2. 系統狀態檢查

```bash
# 查看完整系統狀態
bash check_system_status.sh

# 顯示：
# - Docker 容器狀態
# - Neo4j 資料統計
# - ChromaDB 向量數量
# - Ollama 模型列表
# - 資源使用情況
```

---

## 🎨 推薦模型選擇

### 根據任務選擇最佳模型

| 任務類型 | 推薦模型 | 特點 |
|---------|---------|------|
| 複雜研究 | `llama31-70b-enhanced-rag` | 70B旗艦級，最全面 |
| 中文問題 | `qwen3-30b-enhanced-rag` | 30B中文專精 |
| 邏輯推理 | `deepseek-32b-agentic-rag` | 32B推理專家 |
| 創意分析 | `gpt-oss-20b-advanced-rag` | 20B創意模型 |
| 快速問答 | `llama31-8b-naive-rag` | 8B快速響應 |

---

## ❓ 常見問題

### Q: 更新資料需要多久生效？

**A**: ⚡ **立即生效**（0 秒等待）

```
T0: 爬蟲完成 → 資料寫入資料庫
T1: 在 OpenWebUI 提問 → 即時使用新資料 ✅
```

### Q: 需要重啟什麼嗎？

**A**: ❌ **完全不需要**

資料庫會自動反映最新狀態，所有服務保持運行即可。

### Q: 如何確認使用了最新資料？

**A**: 提問測試

```
問: "資料庫中有多少件藝術品？"
回答應反映實際數量
```

### Q: 會有緩存問題嗎？

**A**: ❌ **不會**

系統每次都實時查詢資料庫，不依賴緩存。

---

## 📅 定期更新建議

### 設置自動更新（可選）

```bash
# 添加到 crontab
crontab -e

# 每週日凌晨 2 點自動更新
0 2 * * 0 cd /path/to/art-history-database && python3 crawl_renaissance_baroque.py >> /tmp/crawler.log 2>&1
```

---

## 🎉 總結

| 項目 | 狀態 | 說明 |
|-----|------|------|
| 資料自動更新 | ✅ 是 | 實時查詢資料庫 |
| 需要重新創建模型 | ❌ 否 | 模型不含資料 |
| 需要重啟服務 | ❌ 否 | 即時生效 |
| 更新延遲 | ⚡ 0秒 | 立即可用 |

### 你只需要做

```bash
1. 運行爬蟲: python3 crawl_renaissance_baroque.py
2. 使用 OpenWebUI: http://localhost:8080
3. 享受最新資料 ✅
```

就這麼簡單！🎨✨

---

## 📚 相關文件

- `資料更新機制說明.md` - 完整技術原理
- `update_art_history_data.sh` - 更新腳本
- `check_system_status.sh` - 狀態檢查
- `multi-database-rag-server.js` - RAG 查詢服務器

---

**最後更新**: 2025-11-05
**系統版本**: v5.0
