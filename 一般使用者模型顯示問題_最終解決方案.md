# 一般使用者模型顯示問題 - 最終解決方案

**日期**: 2025-12-11
**狀態**: 🔧 已完成所有後端修復，等待前端驗證
**問題**: 一般使用者在模型選擇器中看不到任何模型（管理員可以看到）

---

## 📊 當前狀態總結

### ✅ 已修復的後端問題

1. **模型訪問權限** - ✅ 已完成
   - 所有 51 個模型的 `access_control` 已設為 `{}` (公開)
   - 更新時間: 2025-12-11 10:47

2. **模型激活狀態** - ✅ 已完成
   - 所有 51 個模型的 `is_active` 已設為 `1` (激活)
   - 之前有 1 個模型 (`gemma2:2b`) 未激活，現已修復
   - 更新時間: 2025-12-11 03:00

3. **OpenWebUI 服務** - ✅ 已重啟
   - 服務已重啟以應用所有數據庫更改
   - 狀態: 正常運行

### 🔍 數據庫驗證結果

```sql
-- 所有模型的訪問控制狀態
SELECT COUNT(*) FROM model WHERE access_control = '{}';
-- 結果: 51 (全部公開)

-- 所有模型的激活狀態
SELECT COUNT(*) FROM model WHERE is_active = 1;
-- 結果: 51 (全部激活)

-- 使用者資訊
SELECT id, email, role FROM user;
-- 管理員: ssking1999@gmail.com (admin)
-- 一般使用者: ssskyjosh1999@gmail.com (user)
```

### 📝 API 日誌分析

根據 OpenWebUI 日誌，一般使用者的 API 調用成功:

```
2025-12-11 02:55:17.856 | "GET /api/models HTTP/1.1" 200
2025-12-11 02:55:29.424 | "GET /api/models HTTP/1.1" 200
2025-12-11 02:55:30.939 | "GET /api/models HTTP/1.1" 200
2025-12-11 02:55:48.104 | "GET /api/models HTTP/1.1" 200
```

**重要發現**: API 返回 HTTP 200，表示後端成功返回模型列表給一般使用者

---

## 🎯 一般使用者必須執行的步驟

### ⚠️ 關鍵:這是前端快取問題!

由於後端已正確配置，但前端仍未顯示模型，這很可能是瀏覽器快取問題。請嚴格按照以下步驟操作:

### 步驟 1: 完全清除瀏覽器數據

#### Chrome/Edge 使用者:

1. **完全關閉 OpenWebUI 標籤頁**

2. **開啟清除瀏覽數據對話框**:
   - 按 `Ctrl + Shift + Delete`

3. **選擇以下選項**:
   - **Time range**: `All time` (全部時間) ⚠️ 必須選擇
   - 勾選: `Cookies and other site data` (Cookie 和其他網站數據)
   - 勾選: `Cached images and files` (快取的圖片和文件)
   - 勾選: `Hosted app data` (託管應用數據) - 如果有的話

4. **點擊**: `Clear data` (清除數據)

5. **等待清除完成**: 確認看到 "數據已清除" 或類似訊息

#### Firefox 使用者:

1. `Ctrl + Shift + Delete`
2. Time range: `Everything` (全部)
3. 勾選: Cookies, Cache, Site Preferences
4. 點擊: `Clear Now`

### 步驟 2: 關閉並重新開啟瀏覽器

**重要**: 不是只關閉標籤頁，而是完全關閉瀏覽器

1. 關閉所有瀏覽器視窗
2. 等待 5 秒
3. 重新開啟瀏覽器

### 步驟 3: 無痕模式測試 (推薦)

為了排除快取問題，建議先用無痕模式測試:

1. **開啟無痕視窗**:
   - Chrome/Edge: `Ctrl + Shift + N`
   - Firefox: `Ctrl + Shift + P`

2. **訪問 OpenWebUI**:
   ```
   http://localhost:8080
   ```

3. **登入一般使用者帳號**:
   - Email: ssskyjosh1999@gmail.com
   - Password: (您的密碼)

4. **檢查模型選擇器**:
   - 點擊: `New Chat` (新對話)
   - 點擊左上角的模型選擇器
   - 應該看到 **51 個模型**!

### 步驟 4: 如果無痕模式可以看到模型

如果在無痕模式下可以看到模型，但正常模式看不到:

1. 回到正常瀏覽器
2. 再次執行步驟 1 (清除快取)
3. 額外清除以下內容:
   - `Site Settings` (網站設定)
   - `Autofill form data` (自動填入表單數據)
4. 重新登入

### 步驟 5: 檢查開發者工具 (如果還是看不到)

如果以上步驟都無法解決，請提供以下資訊:

#### 5.1 檢查 Console 錯誤

1. 按 `F12` 開啟開發者工具
2. 切換到 `Console` 標籤
3. 刷新頁面 (`F5`)
4. 查看是否有任何 **紅色錯誤訊息**
5. 截圖或複製錯誤訊息

#### 5.2 檢查 Network 請求

1. 在開發者工具中，切換到 `Network` 標籤
2. 勾選 `Preserve log` (保留日誌)
3. 刷新頁面 (`F5`)
4. 在篩選框中輸入: `models`
5. 找到 `/api/models` 請求
6. 點擊該請求，查看:
   - `Headers` 標籤: 確認狀態碼是 200
   - `Preview` 或 `Response` 標籤: 查看返回的模型列表
7. 截圖或複製回應內容

#### 5.3 檢查 Application Storage

1. 在開發者工具中，切換到 `Application` 標籤
2. 左側選單選擇:
   - `Local Storage` → `http://localhost:8080`
   - `Session Storage` → `http://localhost:8080`
3. 檢查是否有任何與模型相關的快取數據
4. 嘗試清除這些儲存 (右鍵 → Clear)

---

## 🔧 後端修復摘要 (已完成)

### 修復 1: 模型訪問權限

**問題**: 所有 RAG 模型的 `access_control` 設為限制性權限
```json
{
  "read": {"group_ids": [], "user_ids": []},
  "write": {"group_ids": [], "user_ids": []}
}
```

**解決方案**: 更新為公開權限
```bash
docker exec art-history-openwebui sqlite3 /app/backend/data/webui.db \
  "UPDATE model SET access_control = '{}' WHERE access_control != '{}';"
```

**結果**: ✅ 所有 51 個模型現在對所有使用者公開

### 修復 2: 模型激活狀態

**問題**: 1 個模型 (`gemma2:2b`) 的 `is_active = 0`

**解決方案**:
```bash
docker exec art-history-openwebui sqlite3 /app/backend/data/webui.db \
  "UPDATE model SET is_active = 1 WHERE is_active = 0;"
```

**結果**: ✅ 所有 51 個模型現在都已激活

### 修復 3: 重啟服務

**操作**:
```bash
docker restart art-history-openwebui
```

**結果**: ✅ OpenWebUI 已重啟並應用所有更改

---

## 📋 可用的 51 個模型列表

### 基礎模型 (11 個):
- llama3.1:latest
- qwen2.5:latest
- deepseek-r1:latest
- gemma2:2b
- ... (其他 7 個)

### RAG 模型 (40 個):

#### 70B 級別 (5 個):
- llama31-70b-naive-rag
- llama31-70b-advanced-rag
- llama31-70b-graph-rag
- llama31-70b-agentic-rag
- llama31-70b-enhanced-rag

#### 30B 級別 (5 個):
- qwen3-30b-naive-rag
- qwen3-30b-advanced-rag
- qwen3-30b-graph-rag
- qwen3-30b-agentic-rag
- qwen3-30b-enhanced-rag

#### 32B 級別 (5 個):
- deepseek-32b-naive-rag
- deepseek-32b-advanced-rag
- deepseek-32b-graph-rag
- deepseek-32b-agentic-rag
- deepseek-32b-enhanced-rag

#### 20B 級別 (5 個):
- gpt-oss-20b-naive-rag
- gpt-oss-20b-advanced-rag
- gpt-oss-20b-graph-rag
- gpt-oss-20b-agentic-rag
- gpt-oss-20b-enhanced-rag

#### 8B 級別 (12 個):
- llama31-naive-rag
- llama31-advanced-rag
- llama31-graph-rag
- llama31-agentic-rag
- qwen3-8b-naive-rag
- qwen3-8b-advanced-rag
- qwen3-8b-graph-rag
- qwen3-8b-agentic-rag
- deepseek-naive-rag
- deepseek-advanced-rag
- deepseek-graph-rag
- deepseek-agentic-rag

#### 4B 級別 (8 個):
- qwen3-naive-rag
- qwen3-advanced-rag
- qwen3-graph-rag
- qwen3-agentic-rag
- gemma3-naive-rag
- gemma3-advanced-rag
- gemma3-graph-rag
- gemma3-agentic-rag

---

## 🐛 疑難排解

### 問題 1: 清除快取後還是看不到模型

**可能原因**:
1. 瀏覽器快取沒有完全清除
2. Local Storage 或 Session Storage 有殘留數據
3. 瀏覽器擴充功能干擾

**解決方案**:
1. 嘗試無痕模式
2. 嘗試不同的瀏覽器 (如果用 Chrome 試試 Firefox)
3. 停用所有瀏覽器擴充功能
4. 手動清除 Application Storage (見上方步驟 5.3)

### 問題 2: 無痕模式可以看到，正常模式看不到

**原因**: 瀏覽器 Profile 中有損壞的快取數據

**解決方案**:
1. 重新建立瀏覽器 Profile
2. 或使用瀏覽器的 "重設設定" 功能

### 問題 3: 所有瀏覽器都看不到

**原因**: 可能是 OpenWebUI 前端代碼的問題

**解決方案**:
1. 檢查 OpenWebUI 版本是否最新
2. 查看 Console 是否有 JavaScript 錯誤
3. 檢查 Network 標籤確認 API 是否返回模型數據

---

## 📊 驗證清單

在聯繫支援前，請確認以下所有項目:

- [ ] 已在 Chrome/Edge 中按 `Ctrl + Shift + Delete` 清除快取
- [ ] Time range 選擇了 `All time`
- [ ] 已勾選 Cookies、Cache、Hosted app data
- [ ] 已完全關閉並重新開啟瀏覽器
- [ ] 已嘗試無痕模式登入
- [ ] 已檢查 F12 → Console 是否有錯誤
- [ ] 已檢查 F12 → Network → /api/models 的回應
- [ ] 已嘗試不同的瀏覽器
- [ ] 已確認使用一般使用者帳號 (ssskyjosh1999@gmail.com) 登入

---

## 🎉 預期結果

完成以上步驟後，一般使用者應該能夠:

1. ✅ 在模型選擇器中看到 **51 個模型**
2. ✅ 看到所有基礎模型 (11 個)
3. ✅ 看到所有 RAG 模型 (40 個)
4. ✅ 能夠選擇任何模型建立新對話
5. ✅ 能夠正常使用 RAG 模型進行對話

---

## 📞 如需進一步支援

如果完成所有步驟後仍然無法看到模型，請提供:

1. **瀏覽器資訊**:
   - 瀏覽器名稱和版本
   - 作業系統

2. **Console 錯誤** (F12 → Console):
   - 截圖所有紅色錯誤訊息

3. **Network 回應** (F12 → Network → models):
   - `/api/models` 請求的狀態碼
   - Response 內容的截圖

4. **測試結果**:
   - 無痕模式是否可以看到模型?
   - 不同瀏覽器是否可以看到模型?

---

## 🔍 技術背景

### 為什麼會發生這個問題?

1. **權限問題** (已修復):
   - OpenWebUI 在同步 Ollama 模型時，自動設定了限制性的 `access_control`
   - 空的 `user_ids` 數組意味著只有創建者可以訪問

2. **激活問題** (已修復):
   - 1 個模型未激活，可能導致前端渲染問題

3. **快取問題** (待用戶驗證):
   - 瀏覽器快取了舊的模型列表 (空列表)
   - 即使後端已修復，前端仍顯示快取的空列表

### OpenWebUI 的模型權限機制

```json
// 公開模型 (所有使用者可見)
{"access_control": "{}"}

// 限制模型 (只有創建者和白名單可見)
{
  "access_control": {
    "read": {"group_ids": [], "user_ids": []},
    "write": {"group_ids": [], "user_ids": []}
  }
}
```

---

**最後更新**: 2025-12-11 03:00
**狀態**: 🟢 後端已完全修復，等待前端驗證
**下一步**: 一般使用者執行清除快取步驟
