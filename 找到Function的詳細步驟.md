# 🔍 在 OpenWebUI 中找到 Function 的詳細步驟

**目標**: 找到並編輯藝術史 RAG+LLM Function

---

## 📍 方法 1: 通過左側菜單（最常見）

### 步驟 1: 打開 OpenWebUI
在瀏覽器訪問：**http://localhost:8080**

### 步驟 2: 查找左側菜單
看螢幕**左上角**，應該會看到以下之一：
- ☰ 三條線圖標（漢堡菜單）
- 已經展開的側邊欄

### 步驟 3: 展開菜單（如果需要）
如果只看到 ☰ 圖標，**點擊它**

### 步驟 4: 尋找這些選項（依序嘗試）

**選項 A: Workspace**
```
☰ 菜單
  ├─ Home (首頁)
  ├─ Workspace ← 點這裡！
  │   ├─ Models (模型)
  │   ├─ Functions ← 然後點這裡！
  │   ├─ Tools (工具)
  │   └─ ...
  └─ ...
```

**選項 B: Admin Settings（如果您是管理員）**
```
☰ 菜單
  ├─ Home (首頁)
  ├─ Settings (設置)
  │   └─ Admin Settings (管理員設置)
  │       └─ Functions ← 點這裡！
  └─ ...
```

**選項 C: 齒輪圖標**
```
☰ 菜單底部
  └─ ⚙️ (齒輪圖標) ← 點擊
      └─ Functions ← 點這裡！
```

---

## 📍 方法 2: 直接訪問 URL

如果找不到菜單選項，試試直接訪問以下 URL（依序嘗試）：

```
http://localhost:8080/workspace/functions
http://localhost:8080/admin/functions
http://localhost:8080/functions
http://localhost:8080/workspace/tools
```

在瀏覽器地址欄輸入上面任一 URL，按 Enter

---

## 🔍 找到 Function 後

### 您應該會看到 Function 列表頁面

頁面標題可能是：
- **Functions**
- **功能**
- **工具**
- **Tools**

### 尋找藝術史 Function

在列表中找名稱包含以下任一的項目：
- ✅ "藝術史"
- ✅ "RAG+LLM"
- ✅ "Art History"
- ✅ "完整智能組合"
- ✅ "v4.0" 或 "v4"

**完整名稱應該類似**:
```
藝術史 RAG+LLM 完整智能組合系統 v4.0
```

### Function 的外觀特徵

可能顯示為：
```
┌─────────────────────────────────────┐
│ 藝術史 RAG+LLM 完整智能組合系統 v4.0  │
│                                     │
│ 支援5種LLM模型 × 8種RAG策略          │
│                                     │
│ [編輯] [刪除] [...]                  │
└─────────────────────────────────────┘
```

或：
```
• 藝術史 RAG+LLM 完整智能組合系統 v4.0
  作者: Art History Database Team
  [✏️ Edit] [🗑️ Delete]
```

---

## ✏️ 編輯 Function

### 找到後，點擊其中一個按鈕：
- **✏️ Edit（編輯）**
- **📝 編輯**
- 直接**點擊 Function 名稱**

### 您應該會進入編輯頁面

編輯頁面包含：
```
┌─────────────────────────────────────┐
│ Title (標題):                        │
│ [藝術史 RAG+LLM 完整智能組合系統 v4.0]│
│                                     │
│ Description (描述):                  │
│ [支援5種LLM模型...]                  │
│                                     │
│ Content (內容/代碼):                 │
│ ┌─────────────────────────────────┐ │
│ │ """                             │ │
│ │ title: 藝術史 RAG+LLM...         │ │
│ │ """                             │ │
│ │                                 │ │
│ │ import json                     │ │
│ │ import requests                 │ │
│ │ ...                             │ │
│ └─────────────────────────────────┘ │
│                                     │
│ [保存] [取消]                        │
└─────────────────────────────────────┘
```

---

## 🎯 更新代碼的步驟

### 1. 在編輯頁面中，找到 **Content** 或 **代碼** 區域

這是一個大的文本框，包含 Python 代碼

### 2. 全選舊代碼
點擊代碼框內，然後：
- **Windows**: `Ctrl + A`
- **Mac**: `Cmd + A`

### 3. 複製新代碼
打開文件：`enhanced_openwebui_rag_function_v4.py`
- **全選**: `Ctrl + A` (Windows) 或 `Cmd + A` (Mac)
- **複製**: `Ctrl + C` (Windows) 或 `Cmd + C` (Mac)

### 4. 貼上新代碼
回到 OpenWebUI 編輯頁面：
- **貼上**: `Ctrl + V` (Windows) 或 `Cmd + V` (Mac)

### 5. 保存
點擊 **"保存"** 或 **"Save"** 按鈕

---

## ❓ 如果還是找不到

### 檢查 1: 確認您已登入 OpenWebUI
確保您在 http://localhost:8080 已經登入

### 檢查 2: 確認您的帳號權限
有些 OpenWebUI 設定中，Functions 只對管理員可見

### 檢查 3: 查看所有頁面
逐一點擊左側菜單的每個選項，看看是否有 Functions 相關的項目

### 檢查 4: 搜索功能
有些 OpenWebUI 版本有搜索功能，試試搜索：
- "function"
- "藝術史"
- "RAG"

---

## 🆘 替代方案：告訴我您看到什麼

如果您還是找不到，請告訴我：

1. **您訪問 http://localhost:8080 後，看到哪些菜單選項？**
   - 例如：Home, Chat, Models, Settings...

2. **左側菜單看起來像什麼？**
   - 有哪些圖標？
   - 有哪些文字？

3. **頁面頂部有什麼？**
   - 有沒有導航欄？
   - 有哪些按鈕？

4. **OpenWebUI 版本**
   - 如果您知道的話

把您看到的告訴我，我會根據您的界面提供更具體的指引！

---

## 💡 小技巧

如果您能**截圖** OpenWebUI 的主界面，可以：
1. 把截圖保存到桌面
2. 告訴我截圖的位置
3. 我可以看圖片並告訴您具體要點哪裡

---

**祝您順利找到！如果遇到困難，隨時告訴我您看到什麼！**
