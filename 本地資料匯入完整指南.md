# 🎨 本地資料匯入完整指南

**版本**: v1.0
**更新日期**: 2025-01-30
**用途**: 將本地PDF、JSON、TXT等檔案匯入到藝術史RAG系統

---

## 📋 目錄

1. [系統概述](#系統概述)
2. [快速開始](#快速開始)
3. [支援的格式](#支援的格式)
4. [使用方法](#使用方法)
5. [進階功能](#進階功能)
6. [常見問題](#常見問題)

---

## 🎯 系統概述

### 功能特色

✅ **多格式支援** - PDF、JSON、TXT、MD等
✅ **智能提取** - 自動識別標題、藝術家、時期等資訊
✅ **自動整合** - 直接匯入Neo4j和ChromaDB
✅ **即時生效** - 匯入後立即可在OpenWebUI使用
✅ **三種介面** - CLI命令列、Web介面、Python API
✅ **可擴展性** - 易於添加新格式支援

### 系統架構

```
本地檔案 (PDF/JSON/TXT)
    ↓
【檔案處理器】
├─ PDFProcessor     - 提取PDF內容
├─ JSONProcessor    - 解析JSON資料
└─ TextProcessor    - 處理文字檔
    ↓
【資料標準化】
├─ 欄位對應
├─ 時期偵測
└─ 關鍵詞提取
    ↓
【資料庫匯入】
├─ Neo4j (圖資料庫)
└─ ChromaDB (向量資料庫)
    ↓
【RAG系統】
└─ 在OpenWebUI中立即可用
```

---

## 🚀 快速開始

### 1. 安裝依賴

```bash
cd art-history-database

# 基礎依賴 (必須)
pip install neo4j chromadb requests flask

# PDF支援 (選擇性，建議安裝兩者)
pip install PyPDF2        # 基礎PDF處理
pip install pdfplumber    # 進階PDF處理 (推薦)
```

### 2. 檢查服務狀態

確保以下服務正在運行:

```bash
# 檢查Neo4j
docker ps | grep neo4j

# 檢查ChromaDB
curl http://localhost:8001/api/v1/heartbeat

# 檢查Ollama (用於生成向量嵌入)
curl http://localhost:11434/api/version
```

### 3. 建立範例模板

```bash
# 建立各種格式的範例模板
python import_local_data.py --create-template
```

這會在 `./import_templates/` 目錄建立以下檔案:
- `template.json` - JSON格式範例
- `template_structured.txt` - 結構化文字範例
- `template_markdown.md` - Markdown格式範例
- `template_separated.txt` - 分隔符格式範例

### 4. 匯入您的第一個檔案

```bash
# 方法1: 使用CLI (推薦)
python import_local_data.py -f my_artwork.json

# 方法2: 使用Web介面
python web_import_server.py
# 然後打開瀏覽器: http://localhost:5050
```

---

## 📁 支援的格式

### 1. JSON檔案 (.json, .jsonl)

#### 格式A: 陣列格式
```json
[
  {
    "title": "蒙娜麗莎",
    "artist": "Leonardo da Vinci",
    "date": "1503-1519",
    "period": "文藝復興",
    "description": "最著名的肖像畫..."
  },
  {
    "title": "夜巡",
    "artist": "Rembrandt",
    "date": "1642"
  }
]
```

#### 格式B: 包裝格式
```json
{
  "artworks": [
    {
      "title": "蒙娜麗莎",
      "artist": "Leonardo da Vinci"
    }
  ]
}
```

#### 格式C: 單一物件
```json
{
  "title": "蒙娜麗莎",
  "artist": "Leonardo da Vinci",
  "date": "1503-1519"
}
```

#### 格式D: JSONL (每行一個JSON)
```jsonl
{"title": "蒙娜麗莎", "artist": "Leonardo da Vinci"}
{"title": "夜巡", "artist": "Rembrandt"}
```

### 2. 文字檔 (.txt, .md)

#### 格式A: 結構化格式
```
標題: 蒙娜麗莎
藝術家: Leonardo da Vinci
日期: 1503-1519
時期: 文藝復興
描述: 最著名的肖像畫...

標題: 夜巡
藝術家: Rembrandt
日期: 1642
```

#### 格式B: Markdown格式
```markdown
# 蒙娜麗莎

作者: Leonardo da Vinci
年代: 1503-1519

《蒙娜麗莎》是最著名的肖像畫...

---

# 夜巡

作者: Rembrandt
年代: 1642
```

#### 格式C: 分隔符格式
```
蒙娜麗莎
Leonardo da Vinci
1503-1519
最著名的肖像畫...

---

夜巡
Rembrandt
1642
```

### 3. PDF檔案 (.pdf)

系統會自動從PDF提取文本並嘗試識別:
- 結構化欄位 (標題:、藝術家: 等)
- 段落內容
- 藝術家名稱
- 日期資訊

---

## 💻 使用方法

### 方法1: CLI命令列 (適合批次處理)

#### 基本用法

```bash
# 匯入單一檔案
python import_local_data.py -f artwork.json

# 匯入整個目錄
python import_local_data.py -d ./my_artworks/

# 遞迴處理子目錄
python import_local_data.py -d ./my_artworks/ --recursive
```

#### 選擇儲存位置

```bash
# 只儲存到Neo4j
python import_local_data.py -f artwork.pdf --neo4j-only

# 只儲存到ChromaDB
python import_local_data.py -f artwork.pdf --chromadb-only

# 只處理檔案不儲存 (測試用)
python import_local_data.py -f artwork.pdf --no-save
```

#### 自定義資料庫連接

```bash
python import_local_data.py -f artwork.json \
  --neo4j-uri bolt://my-server:7687 \
  --neo4j-user neo4j \
  --neo4j-password mypassword \
  --chromadb-host my-chromadb \
  --chromadb-port 8001
```

#### 完整範例

```bash
# 遞迴匯入整個資料夾，顯示詳細資訊
python import_local_data.py \
  -d ./my_art_collection/ \
  --recursive \
  --verbose
```

### 方法2: Web介面 (適合個別檔案)

#### 啟動服務器

```bash
python web_import_server.py
```

輸出:
```
🎨 藝術史資料匯入 Web 服務器
🌐 服務器啟動中...
📍 URL: http://localhost:5050
```

#### 使用步驟

1. 在瀏覽器打開 `http://localhost:5050`
2. 拖放檔案到上傳區域 (或點擊選擇)
3. 選擇匯入選項 (Neo4j/ChromaDB)
4. 點擊「開始匯入」
5. 查看匯入結果

#### 特點

- 🎨 **美觀介面** - 現代化拖放設計
- 📦 **批次上傳** - 一次上傳多個檔案
- ⚡ **即時回饋** - 顯示處理進度
- 📊 **統計資訊** - 顯示匯入統計

### 方法3: Python API (適合程式整合)

```python
from pathlib import Path
from src.importer import UniversalDataImporter

# 建立匯入器
importer = UniversalDataImporter(
    neo4j_uri="bolt://localhost:7687",
    neo4j_user="neo4j",
    neo4j_password="arthistory123",
    chromadb_host="localhost",
    chromadb_port=8001
)

# 匯入單一檔案
artworks = importer.import_file(
    Path("my_artwork.json"),
    save_to_neo4j=True,
    save_to_chromadb=True
)

print(f"匯入了 {len(artworks)} 件作品")

# 匯入目錄
all_artworks = importer.import_directory(
    Path("./my_artworks/"),
    recursive=True,
    save_to_neo4j=True,
    save_to_chromadb=True
)

# 顯示統計
importer.print_stats()
```

---

## 🔧 進階功能

### 1. 自定義檔案處理器

如果需要支援新的格式,可以擴展處理器:

```python
from src.importer.base_processor import BaseProcessor
from pathlib import Path
from typing import List, Dict, Any

class CSVProcessor(BaseProcessor):
    """CSV處理器範例"""

    def __init__(self):
        super().__init__()
        self.supported_extensions = ['.csv']

    def can_process(self, file_path: Path) -> bool:
        return file_path.suffix.lower() in self.supported_extensions

    def process(self, file_path: Path) -> List[Dict[str, Any]]:
        # 實作CSV處理邏輯
        import csv
        artworks = []

        with open(file_path, 'r', encoding='utf-8') as f:
            reader = csv.DictReader(f)
            for row in reader:
                artwork = self.standardize_data(row)
                if self.validate_data(artwork):
                    artworks.append(artwork)

        return artworks

# 註冊到匯入器
from src.importer import UniversalDataImporter
importer = UniversalDataImporter()
importer.processors.append(CSVProcessor())
```

### 2. 監控資料夾自動匯入

建立一個監控腳本,自動匯入新檔案:

```python
import time
from pathlib import Path
from watchdog.observers import Observer
from watchdog.events import FileSystemEventHandler
from src.importer import UniversalDataImporter

class ArtworkImportHandler(FileSystemEventHandler):
    def __init__(self, importer):
        self.importer = importer

    def on_created(self, event):
        if event.is_directory:
            return

        file_path = Path(event.src_path)
        print(f"偵測到新檔案: {file_path.name}")

        # 匯入
        artworks = self.importer.import_file(file_path)
        print(f"成功匯入 {len(artworks)} 件作品")

# 使用
importer = UniversalDataImporter()
event_handler = ArtworkImportHandler(importer)
observer = Observer()
observer.schedule(event_handler, path='./watch_folder/', recursive=True)
observer.start()

try:
    while True:
        time.sleep(1)
except KeyboardInterrupt:
    observer.stop()
observer.join()
```

### 3. 批次處理範例

```bash
#!/bin/bash
# 批次匯入多個目錄

echo "開始批次匯入..."

# 匯入文藝復興資料
python import_local_data.py -d ./renaissance/ --recursive

# 匯入巴洛克資料
python import_local_data.py -d ./baroque/ --recursive

# 匯入現代藝術資料
python import_local_data.py -d ./modern/ --recursive

echo "批次匯入完成!"
```

---

## ❓ 常見問題

### Q1: 支援哪些欄位?

**A**: 系統支援以下欄位 (只有`title`是必填):

**核心欄位**:
- `title` - 作品標題 (必填)
- `artist` - 藝術家名稱
- `date` - 創作日期
- `description` - 作品描述

**進階欄位**:
- `period` - 藝術時期 (如: 文藝復興)
- `style` - 藝術風格
- `medium` - 媒材/材質
- `dimensions` - 尺寸
- `location` - 館藏地點
- `keywords` - 關鍵詞陣列
- `image_url` - 圖片連結

### Q2: 系統如何處理重複資料?

**A**: 系統會:
1. 根據 `title + artist` 生成唯一ID
2. 使用 `MERGE` 語法避免重複
3. 如果已存在,會更新資料而非建立新紀錄

### Q3: 匯入的資料何時可以在OpenWebUI使用?

**A**: **立即可用!**

資料匯入到Neo4j和ChromaDB後,RAG系統會實時查詢,無需:
- ❌ 重建模型
- ❌ 重啟服務
- ❌ 清除快取

### Q4: 如何驗證資料已成功匯入?

```bash
# 方法1: 查詢Neo4j節點數
docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n:Artwork) WHERE n.source = 'local_import' RETURN count(n)"

# 方法2: 在OpenWebUI測試
# 提問: "資料庫中有哪些本地匯入的作品?"
```

### Q5: PDF處理失敗怎麼辦?

**A**:
1. **安裝完整PDF支援**:
   ```bash
   pip install PyPDF2 pdfplumber
   ```

2. **檢查PDF是否可提取文本**:
   - 某些掃描PDF是圖片,無法提取文本
   - 考慮使用OCR工具先轉換

3. **手動轉換**:
   - 將PDF內容複製到TXT或JSON檔案

### Q6: 如何處理非結構化的文本資料?

**A**: 系統會自動:
1. 使用檔名作為標題
2. 從內容提取藝術家和日期
3. 偵測藝術時期和關鍵詞
4. 將全文作為描述

或者,使用結構化格式範本重新整理資料。

### Q7: 可以刪除匯入的資料嗎?

**A**: 可以,使用Cypher查詢:

```bash
# 刪除所有本地匯入的資料
docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n:Artwork {source: 'local_import'}) DETACH DELETE n"

# 刪除特定作品
docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n:Artwork {title: '作品名稱'}) DETACH DELETE n"
```

### Q8: 系統對檔案大小有限制嗎?

**A**:
- **CLI**: 無限制
- **Web介面**: 預設50MB,可在`web_import_server.py`修改

### Q9: 匯入速度慢怎麼辦?

**A**:
1. **批次匯入**: 使用CLI而非Web介面
2. **關閉ChromaDB**: 如果只需圖譜,使用 `--neo4j-only`
3. **檢查Ollama**: 確保Ollama服務正常運行

### Q10: 如何更新已匯入的資料?

**A**: 直接重新匯入即可,系統會自動更新現有紀錄。

---

## 📊 完整工作流程範例

### 情境: 匯入一批文藝復興PDF資料

```bash
# 步驟1: 建立專案資料夾
mkdir ~/renaissance_artworks
cd ~/renaissance_artworks

# 步驟2: 放置PDF檔案
# (將您的PDF檔案複製到此目錄)

# 步驟3: 檢查服務狀態
cd ~/Desktop/藝術史資料庫/art-history-database
docker ps | grep -E "neo4j|chromadb"

# 步驟4: 安裝PDF支援
pip install pdfplumber PyPDF2

# 步驟5: 執行匯入
python import_local_data.py \
  -d ~/renaissance_artworks \
  --recursive \
  --verbose

# 步驟6: 驗證匯入
# 在OpenWebUI (http://localhost:8080) 中提問:
# "最近匯入了哪些文藝復興作品?"

# 步驟7: 查看統計
docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n:Artwork) WHERE n.source = 'local_pdf_import'
   RETURN count(n) as total,
          collect(n.title)[0..5] as sample_titles"
```

---

## 🎉 總結

### 核心優勢

1. **✅ 多格式支援** - PDF、JSON、TXT一鍵匯入
2. **✅ 智能處理** - 自動提取和標準化
3. **✅ 即時可用** - 匯入後立即在RAG系統中使用
4. **✅ 三種介面** - CLI、Web、Python API任選
5. **✅ 可擴展** - 易於添加新格式支援

### 快速參考

```bash
# 建立模板
python import_local_data.py --create-template

# 匯入檔案
python import_local_data.py -f my_file.json

# 匯入目錄
python import_local_data.py -d ./my_data/ --recursive

# 啟動Web介面
python web_import_server.py
```

### 下一步

- 📖 查看 `import_templates/` 中的範例
- 🌐 啟動Web介面體驗拖放上傳
- 🔧 根據需求自定義處理器
- 📊 在OpenWebUI中測試匯入的資料

---

**文檔版本**: v1.0
**最後更新**: 2025-01-30
**維護者**: Art History Database Team
