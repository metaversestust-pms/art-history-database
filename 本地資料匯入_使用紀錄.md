# 📝 本地資料匯入系統 - 使用紀錄

**建立日期**: 2025-01-30
**系統狀態**: ✅ 已完成並成功測試

---

## ✅ 已完成工作

### 1. 系統開發
- ✅ 建立模組化處理器架構 (BaseProcessor, PDFProcessor, JSONProcessor, TextProcessor)
- ✅ 開發CLI命令列工具 (import_local_data.py)
- ✅ 整合Neo4j圖資料庫
- ✅ 整合ChromaDB向量資料庫
- ✅ 實作Ollama嵌入向量生成
- ✅ 建立完整文檔系統

### 2. 依賴安裝
```bash
# 已安裝的套件
pip3 install --break-system-packages PyPDF2 pdfplumber neo4j chromadb requests
```

### 3. 成功匯入資料
**來源目錄**: `./南藝大及漢寶德校長資料整理/`

**已匯入檔案**:
1. 漢寶德校長生平.pdf
2. 漢寶德紀念館導覽手冊.pdf
3. 認識南藝.pdf
4. 20個測試LLM關於漢寶德的測試提問及簡短答案.txt
5. 專用字.txt
6. 通用字.txt

**匯入結果**:
- 總檔案數: 6
- 成功處理: 6 ✅
- 提取作品數: 6
- 儲存到Neo4j: 6 ✅
- 儲存到ChromaDB: 6 ✅

### 4. 系統驗證
```bash
# 已驗證Neo4j中的資料
docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n:Artwork) WHERE n.source CONTAINS 'local' RETURN count(n)"
# 結果: 12 (包含測試匯入的重複資料)
```

---

## 🚀 日常使用指南

### 匯入新資料的標準流程

#### 1. 準備資料
將您的資料檔案放在一個目錄中，支援格式:
- PDF檔案 (.pdf)
- JSON檔案 (.json, .jsonl)
- 文字檔 (.txt, .md)

#### 2. 執行匯入命令
```bash
cd /mnt/c/Users/ssking1999/Desktop/藝術史資料庫/art-history-database

# 匯入單一檔案
python3 import_local_data.py -f 檔案路徑.json --chromadb-port 8000

# 匯入整個目錄
python3 import_local_data.py -d ./資料目錄/ --chromadb-port 8000 --recursive
```

**重要**: 必須加上 `--chromadb-port 8000` 參數

#### 3. 驗證匯入
在OpenWebUI中測試:
1. 開啟 http://localhost:8080
2. 選擇任一RAG模型
3. 提問測試，例如:
   - "資料庫中有哪些本地匯入的作品?"
   - "告訴我關於[您匯入的主題]的資訊"

---

## 📚 常用命令速查

### 基本匯入
```bash
# 匯入JSON檔案
python3 import_local_data.py -f artwork.json --chromadb-port 8000

# 匯入PDF檔案
python3 import_local_data.py -f document.pdf --chromadb-port 8000

# 匯入文字檔
python3 import_local_data.py -f data.txt --chromadb-port 8000
```

### 批次匯入
```bash
# 匯入目錄中所有支援的檔案
python3 import_local_data.py -d ./my_data/ --chromadb-port 8000

# 包含子目錄
python3 import_local_data.py -d ./my_data/ --recursive --chromadb-port 8000
```

### 進階選項
```bash
# 只存到Neo4j
python3 import_local_data.py -f file.json --neo4j-only --chromadb-port 8000

# 只存到ChromaDB
python3 import_local_data.py -f file.json --chromadb-only --chromadb-port 8000

# 測試模式(不實際儲存)
python3 import_local_data.py -f file.json --no-save

# 顯示詳細處理資訊
python3 import_local_data.py -f file.json --verbose --chromadb-port 8000
```

### 建立範例模板
```bash
# 產生範例檔案在 import_templates/ 目錄
python3 import_local_data.py --create-template
```

---

## 🔧 重要設定

### ChromaDB連接埠
**必須使用**: `--chromadb-port 8000`

預設值是8001，但您的ChromaDB服務運行在8000埠，所以每次匯入都要指定。

### 資料庫連接資訊
```python
# Neo4j
URI: bolt://localhost:7687
用戶: neo4j
密碼: arthistory123

# ChromaDB
主機: localhost
埠號: 8000

# Ollama
主機: localhost
埠號: 11434
模型: nomic-embed-text
```

---

## 📁 資料格式範例

### JSON格式 (推薦)
```json
{
  "artworks": [
    {
      "title": "作品標題",
      "artist": "藝術家名稱",
      "date": "創作年代",
      "period": "藝術時期",
      "style": "藝術風格",
      "medium": "媒材",
      "dimensions": "尺寸",
      "location": "收藏地點",
      "description": "作品描述",
      "keywords": ["關鍵字1", "關鍵字2"]
    }
  ]
}
```

### 結構化文字格式
```
標題: 作品標題
藝術家: 藝術家名稱
日期: 創作年代
時期: 藝術時期
描述: 作品描述...

標題: 第二件作品
藝術家: 另一位藝術家
...
```

### Markdown格式
```markdown
# 作品標題

作者: 藝術家名稱
年代: 創作年代
時期: 藝術時期

作品描述...
```

完整範例可查看 `import_templates/` 目錄中的檔案。

---

## 🔍 資料查詢

### 查詢Neo4j中的本地資料
```bash
# 統計本地匯入的作品數量
docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n:Artwork) WHERE n.source CONTAINS 'local' RETURN count(n)"

# 列出所有本地匯入的作品標題
docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n:Artwork) WHERE n.source CONTAINS 'local' RETURN n.title LIMIT 10"

# 查看特定作品的完整資訊
docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123 \
  "MATCH (n:Artwork) WHERE n.title CONTAINS '關鍵字' RETURN n"
```

### 檢查服務狀態
```bash
# 檢查Docker服務
docker ps | grep -E "neo4j|chromadb|ollama"

# 啟動所有服務
docker-compose up -d

# 查看服務日誌
docker-compose logs -f neo4j
docker-compose logs -f chromadb
```

---

## ⚠️ 常見問題處理

### 問題1: ChromaDB連接失敗
**錯誤**: `Could not connect to a Chroma server`

**解決**:
```bash
# 檢查ChromaDB是否運行
docker ps | grep chromadb

# 確認使用正確的埠號
python3 import_local_data.py -f file.json --chromadb-port 8000
```

### 問題2: PDF處理失敗
**錯誤**: `無法處理PDF: 缺少 PyPDF2 或 pdfplumber`

**解決**:
```bash
pip3 install --break-system-packages PyPDF2 pdfplumber
```

### 問題3: 資料庫套件未安裝
**錯誤**: `資料庫套件未安裝，僅支援檔案處理`

**解決**:
```bash
pip3 install --break-system-packages neo4j chromadb requests
```

### 問題4: Neo4j連接失敗
**解決**:
```bash
# 重啟Neo4j服務
docker-compose restart neo4j

# 檢查密碼是否正確
docker exec art-history-neo4j cypher-shell -u neo4j -p arthistory123
```

---

## 📖 文檔索引

### 快速開始
- **本地資料匯入_README.md** - 系統總覽
- **快速開始_無需Web介面.md** - CLI快速上手指南
- **本地資料匯入_快速參考.md** - 命令速查表

### 詳細文檔
- **本地資料匯入完整指南.md** - 完整功能說明
- **本地資料匯入系統架構圖.md** - 系統架構說明
- **安裝Web依賴.md** - Web介面安裝指南

### 範例檔案
- **import_templates/template.json** - JSON格式範例
- **import_templates/template_structured.txt** - 結構化文字範例
- **import_templates/template_markdown.md** - Markdown範例

---

## 🎯 最佳實踐建議

### 1. 資料整理
- 使用JSON格式可獲得最佳結構化支援
- 為每件作品提供盡可能完整的欄位
- 在description中提供詳細描述以提升RAG檢索品質

### 2. 批次匯入
- 將同類型資料放在同一目錄
- 使用有意義的檔案名稱
- 先測試單一檔案，確認格式正確後再批次處理

### 3. 資料驗證
- 匯入後立即在OpenWebUI中測試
- 定期查詢Neo4j確認資料完整性
- 使用 `--verbose` 選項檢視處理細節

### 4. 維護建議
- 定期備份Neo4j和ChromaDB資料
- 記錄每次匯入的來源和日期
- 避免重複匯入相同資料

---

## 📊 系統特色

✅ **即時可用** - 匯入後立即在RAG系統中可查詢
✅ **多格式支援** - PDF、JSON、TXT、Markdown
✅ **智能處理** - 自動欄位提取、時期判斷、關鍵字生成
✅ **雙資料庫** - Neo4j圖資料庫 + ChromaDB向量資料庫
✅ **批次處理** - 支援目錄遞迴匯入
✅ **靈活配置** - 可選擇儲存目標、測試模式等

---

## 🎉 成功案例

### 南藝大及漢寶德校長資料
**匯入日期**: 2025-01-30
**檔案數量**: 6 (3 PDF + 3 TXT)
**處理結果**: 全部成功 ✅
**資料用途**: 建立南藝大和漢寶德校長相關的知識庫

**測試查詢**:
- "資料庫中有關於漢寶德校長的資料嗎?"
- "南藝大的歷史和特色是什麼?"
- "漢寶德紀念館有哪些展覽內容?"

這些查詢現在都可以在OpenWebUI中使用RAG模型獲得基於本地匯入資料的回答。

---

## 📞 技術支援

### 查看幫助
```bash
python3 import_local_data.py --help
```

### 系統資訊
- Python版本: 3.x (需支援 f-strings)
- 作業系統: Linux (WSL2)
- Docker: 必須運行 Neo4j 和 ChromaDB 容器
- Ollama: 必須運行並載入 nomic-embed-text 模型

### 重要路徑
```
專案根目錄: /mnt/c/Users/ssking1999/Desktop/藝術史資料庫/art-history-database
匯入工具: import_local_data.py
處理器模組: src/importer/
範例模板: import_templates/
文檔目錄: *.md 檔案
```

---

**最後更新**: 2025-01-30
**系統版本**: v1.0
**維護狀態**: ✅ 生產就緒

**下次使用時的快速命令**:
```bash
cd /mnt/c/Users/ssking1999/Desktop/藝術史資料庫/art-history-database
python3 import_local_data.py -d ./您的資料目錄/ --chromadb-port 8000 --recursive
```

祝使用愉快! 🎨✨
