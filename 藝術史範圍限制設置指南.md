# 🎨 藝術史問題範圍限制設置指南

**版本**: v1.0
**更新日期**: 2025-11-05
**功能**: 限制 OpenWebUI 只回答藝術史相關問題

---

## 📋 目錄

1. [功能概述](#功能概述)
2. [工作原理](#工作原理)
3. [設置步驟](#設置步驟)
4. [測試驗證](#測試驗證)
5. [自定義配置](#自定義配置)
6. [故障排除](#故障排除)

---

## 🎯 功能概述

### 實現效果

當使用者提問**非藝術史相關問題**時，系統會：

✅ **自動識別**問題不在範圍內
✅ **友好拒絕**並說明系統專注領域
✅ **提供建議**告訴使用者可以問什麼
✅ **節省資源**不進行無關的RAG檢索

### 範圍限制示例

| 問題類型 | 是否回答 | 系統反應 |
|---------|---------|---------|
| "莫內的睡蓮系列有什麼特色？" | ✅ 回答 | 正常RAG檢索+LLM生成 |
| "Python怎麼連接資料庫？" | ❌ 拒絕 | "本系統專注於藝術史領域..." |
| "今天天氣怎麼樣？" | ❌ 拒絕 | "您的問題不在藝術史範圍內..." |
| "齊白石的蝦畫技法" | ✅ 回答 | 正常RAG檢索+LLM生成 |

---

## 🔧 工作原理

### 三層防護機制

```
使用者提問
    ↓
【層級1】問題範圍預檢
    ├─ 檢查藝術史關鍵詞
    ├─ 檢查藝術家名稱
    ├─ 檢查排除關鍵詞
    └─ 計算相關性置信度
    ↓
置信度 > 30%?
    ├─ NO → 返回友好拒絕訊息 ❌
    └─ YES ↓
【層級2】RAG檢索
    └─ 向Neo4j + ChromaDB查詢
    ↓
找到相關資料?
    ├─ NO → "資料庫中暫無相關資料" ❌
    └─ YES ↓
【層級3】LLM生成回答
    └─ 基於檢索結果生成專業回答 ✅
```

### 關鍵詞分類

**✅ 藝術史核心關鍵詞**:
- 藝術類型: 繪畫、雕塑、建築、書法、攝影
- 流派風格: 文藝復興、巴洛克、印象派、抽象表現主義
- 中國藝術: 國畫、山水畫、青銅器、瓷器
- 藝術家: 達文西、莫內、齊白石、徐悲鴻
- 藝術理論: 美學、藝術史、批評、鑑賞

**❌ 排除關鍵詞**:
- 技術/編程: 代碼、程式、軟體、算法
- 科學: 化學、物理、數學公式、實驗
- 醫療: 疾病、症狀、治療、藥物
- 商業: 股票、投資、財報
- 日常: 菜譜、烹飪、旅遊景點

---

## 📝 設置步驟

### 方法一：使用完整整合腳本（推薦）

#### 步驟 1: 運行整合腳本

```bash
# 進入專案目錄
cd /mnt/c/Users/ssking1999/Desktop/藝術史資料庫/art-history-database

# 運行整合腳本
python3 integrate_scope_filter_to_function.py
```

這個腳本會自動：
- ✅ 讀取現有的 OpenWebUI Function
- ✅ 整合問題範圍過濾器
- ✅ 生成新版本 Function (`enhanced_openwebui_rag_function_v5_with_filter.py`)
- ✅ 保留所有現有功能

#### 步驟 2: 在 OpenWebUI 中更新 Function

1. **訪問 OpenWebUI**
   ```
   http://localhost:8080
   ```

2. **進入 Functions 管理**
   ```
   左上角選單 → Workspace → Functions
   ```

3. **找到現有 Function**
   ```
   名稱: "藝術史 RAG+LLM 完整智能組合系統"
   版本: v4.1.0
   ```

4. **更新 Function 代碼**
   ```
   點擊 "編輯" 按鈕
   複製 enhanced_openwebui_rag_function_v5_with_filter.py 的全部內容
   貼上並保存
   ```

5. **驗證版本**
   ```
   新版本應顯示: v5.0 (含問題範圍限制)
   ```

---

### 方法二：手動整合（適合自定義需求）

#### 步驟 1: 在 Function 中添加過濾器類

打開你的 OpenWebUI Function，在文件開頭添加：

```python
# ===== 藝術史問題範圍過濾器 =====
class ArtHistoryScopeFilter:
    def __init__(self):
        self.core_keywords = {
            "繪畫", "雕塑", "建築", "藝術家", "作品",
            "painting", "sculpture", "artist", "artwork",
            # ... 更多關鍵詞
        }

        self.excluded_keywords = {
            "代碼", "程式", "股票", "疾病",
            "code", "programming", "stock", "disease",
            # ... 更多排除詞
        }

    def is_art_history_related(self, question: str) -> dict:
        # 判斷邏輯（詳見 art_history_scope_filter.py）
        pass

    def get_rejection_message(self, question: str, result: dict) -> str:
        # 生成拒絕訊息（詳見 art_history_scope_filter.py）
        pass
```

#### 步驟 2: 修改 pipe() 函數

在 `pipe()` 函數的開頭添加範圍檢查：

```python
async def pipe(self, body: dict) -> str:
    # 1. 問題範圍檢查
    user_message = body.get("messages", [])[-1].get("content", "")

    filter = ArtHistoryScopeFilter()
    filter_result = filter.is_art_history_related(user_message)

    if not filter_result["is_related"]:
        # 返回友好拒絕訊息
        return filter.get_rejection_message(user_message, filter_result)

    # 2. 通過檢查，繼續正常流程
    # ... 原有的 RAG 檢索邏輯
```

---

## ✅ 測試驗證

### 測試腳本

運行獨立測試：

```bash
python3 art_history_scope_filter.py
```

**預期輸出**:

```
=== 藝術史問題範圍過濾器測試 ===

問題: 莫內的睡蓮系列有什麼特色？
結果: ✅ 通過
置信度: 0.6
理由: 問題包含藝術史相關關鍵詞: 莫內, 藝術家:莫內

------------------------------------------------------------

問題: Python怎麼連接資料庫？
結果: ❌ 拒絕
置信度: 0.0
理由: 問題包含非藝術史領域關鍵詞: 程式, 資料庫
拒絕訊息:
🎨 **藝術史資料庫範圍說明**

抱歉，您的問題「Python怎麼連接資料庫？」包含了非藝術史領域的內容。

檢測到: 問題包含非藝術史領域關鍵詞: 程式, 資料庫

本系統專注於藝術史領域，無法回答其他領域的問題。
```

### 在 OpenWebUI 中測試

1. **測試藝術史相關問題**
   ```
   問: 文藝復興時期有哪些重要藝術家？

   預期: ✅ 系統正常回答，提供達文西、米開朗基羅等資訊
   ```

2. **測試非藝術史問題**
   ```
   問: Python 如何使用列表推導式？

   預期: ❌ 系統拒絕回答
   訊息: "本系統專注於藝術史領域..."
   ```

3. **測試邊緣案例**
   ```
   問: 今天天氣怎麼樣？

   預期: ❌ 系統拒絕回答
   訊息: "您的問題不在藝術史範圍內..."
   ```

---

## 🎛️ 自定義配置

### 調整置信度閾值

在 `ArtHistoryScopeFilter.is_art_history_related()` 中：

```python
# 預設閾值: 0.3 (30%)
is_related = confidence > 0.3

# 更嚴格（減少誤判，但可能拒絕部分相關問題）
is_related = confidence > 0.5

# 更寬鬆（允許更多問題通過，但可能誤判）
is_related = confidence > 0.2
```

### 添加自定義關鍵詞

在 `__init__()` 中添加你的領域特定關鍵詞：

```python
self.core_keywords = {
    # 原有關鍵詞
    "繪畫", "雕塑", ...,

    # 你的自定義關鍵詞
    "裝置藝術", "行為藝術", "數位藝術",
    "installation art", "performance art", "digital art"
}
```

### 修改拒絕訊息

在 `get_rejection_message()` 中自定義訊息內容：

```python
def get_rejection_message(self, question: str, filter_result: dict) -> str:
    return """
    🎨 您好！

    本系統是專門的藝術史資料庫，專注於：
    - 藝術作品與藝術家研究
    - 藝術流派與風格分析
    - 藝術史脈絡與文化背景

    您的問題似乎超出了這個範圍。

    如有藝術史相關問題，歡迎隨時詢問！
    """
```

---

## 🔍 故障排除

### 問題 1: 系統拒絕了明顯的藝術史問題

**症狀**: "達文西是誰？" 被拒絕

**原因**: 關鍵詞庫中缺少 "達文西"

**解決方案**:
```python
# 在 artist_names 中添加
self.artist_names = {
    "達文西", "leonardo",
    # 添加更多藝術家...
}
```

### 問題 2: 系統允許了非藝術史問題

**症狀**: "如何煮咖啡？" 通過了檢查

**原因**:
1. 置信度閾值太低
2. 排除關鍵詞不夠完整

**解決方案**:
```python
# 方案1: 提高閾值
is_related = confidence > 0.4  # 從0.3提高到0.4

# 方案2: 添加排除關鍵詞
self.excluded_keywords = {
    # 原有關鍵詞...
    "咖啡", "烹飪", "食譜",
    "coffee", "cooking", "recipe"
}
```

### 問題 3: 拒絕訊息沒有顯示

**症狀**: 非藝術史問題沒有回答，但也沒有拒絕訊息

**原因**: Function 返回值格式問題

**解決方案**:
```python
# 確保返回字串
async def pipe(self, body: dict) -> str:  # 注意返回類型
    if not filter_result["is_related"]:
        return filter.get_rejection_message(...)  # 直接返回字串
```

### 問題 4: 中文問題無法正確識別

**症狀**: "齊白石的蝦畫" 被拒絕

**原因**:
1. 關鍵詞庫缺少中文藝術家
2. 字符編碼問題

**解決方案**:
```python
# 添加更多中文藝術家
self.artist_names = {
    "齊白石", "張大千", "徐悲鴻", "吳冠中",
    # ... 更多中文藝術家
}

# 確保使用 lower() 進行比對
question_lower = question.lower()
```

---

## 📊 效果評估

### 預期指標

| 指標 | 目標值 | 說明 |
|-----|--------|------|
| **準確率** | > 95% | 正確判斷藝術史問題的比例 |
| **召回率** | > 90% | 成功通過的藝術史問題比例 |
| **誤判率** | < 5% | 非藝術史問題被允許的比例 |
| **響應速度** | < 100ms | 範圍檢查的額外耗時 |

### 優化建議

1. **收集真實問題**
   - 記錄被拒絕的問題
   - 分析是否為誤判
   - 調整關鍵詞庫

2. **A/B測試**
   - 測試不同置信度閾值
   - 比較使用者滿意度
   - 選擇最佳配置

3. **持續迭代**
   - 每月更新關鍵詞庫
   - 添加新的藝術家和流派
   - 優化拒絕訊息

---

## 🎯 最佳實踐

### ✅ DO（推薦做法）

1. **明確的拒絕訊息**
   - 清楚說明系統限制
   - 提供可詢問的問題示例
   - 保持友好禮貌的語氣

2. **合理的置信度閾值**
   - 初始設置為 0.3
   - 根據實際使用調整
   - 平衡準確率和召回率

3. **定期更新關鍵詞**
   - 收集新的藝術家名稱
   - 添加新興藝術流派
   - 移除過時的排除詞

### ❌ DON'T（避免做法）

1. **過於嚴格的過濾**
   - 不要設置過高閾值（> 0.6）
   - 避免拒絕模糊但可能相關的問題

2. **冷漠的拒絕訊息**
   - 不要只說"不回答"
   - 避免機械化的回應

3. **忽略邊緣案例**
   - 不要假設所有問題都明確
   - 處理模糊問題時提供引導

---

## 📚 相關文件

- `art_history_scope_filter.py` - 獨立過濾器模組
- `integrate_scope_filter_to_function.py` - 自動整合腳本
- `enhanced_openwebui_rag_function_v5_with_filter.py` - 整合後的 Function
- `SYSTEM_STATUS_SUMMARY.md` - 系統整體狀態

---

## 🎉 總結

通過這個問題範圍限制機制，您的藝術史資料庫將：

✅ **專注於領域** - 只回答藝術史相關問題
✅ **友好的體驗** - 提供清晰的範圍說明
✅ **節省資源** - 避免無關查詢的資源浪費
✅ **易於維護** - 靈活的關鍵詞配置系統

立即開始設置，讓您的藝術史AI助手更加專業！🎨✨
